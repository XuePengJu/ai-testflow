# PRD：AI 测试工作流平台（ai-testflow）

| 文档信息 | |
|---|---|
| 文档版本 | v2.2 |
| 状态 | 已发布（V2.1 线上运行中，V2.2 开发中） |
| 关联文档 | 《项目1-工作流平台-MVP执行方案.md》（技术实现方案，本文档只写"做什么"，不写"怎么做"） |
| 更新日期 | 2026-08-29 |

---

## 1. 产品概述

### 1.1 一句话定位

面向软件测试工程师的 **AI 测试用例生成平台**：上传接口规格或业务需求文档，AI 自动编排解析 → 生成 → 评审 → 导出四个智能体，产出结构化、可下载的测试用例。

### 1.2 产品背景

- 测试用例编写占测试工作量大头，格式 repetitive、易遗漏边界场景
- 现有 LLM 直接对话生成用例：无质量评审环节、无统一格式、不可沉淀复用
- 本平台将"生成用例"工程化为**可视化工作流**：每个环节有独立 Agent、有质量把关、有标准产出

### 1.3 产品目标

| 目标 | 衡量方式 |
|---|---|
| 用例产出效率提升 | 一份规格文档 → 完整用例集 < 1 分钟（人工需数小时） |
| 用例质量可控 | Reviewer Agent 评审通过才允许导出，附质量报告 |
| 开箱即用 | 未配置大模型 API Key 时自动 Mock 兜底，全流程可演示 |
| 多用户安全 | 注册/访客双通道 + 数据隔离 + 流量加密 |

---

## 2. 用户与角色

### 2.1 角色定义

| 角色 | 来源 | 生命周期 | 定位 |
|---|---|---|---|
| **访客 guest** | 一键体验（按 IP 识别） | 数据保留 24h，到期自动清理 | 低门槛试用，转注册可保留数据 |
| **普通用户 user** | 邮箱注册 | 永久 | 日常使用主力角色 |
| **管理员 admin** | 首个注册用户自动晋升 | 永久 | 平台治理与运营 |

### 2.2 权限矩阵

| 能力 | guest | user | admin |
|---|:---:|:---:|:---:|
| 提交任务 / 查看自己的任务 / 导出下载 | ✅（上限 10 个） | ✅ | ✅ |
| 查看所有用户任务 | ❌ | ❌ | ✅ |
| 修改自己的密码 | —（无密码） | ✅ | ✅ |
| 个人中心 / 切换账户 | ✅ | ✅ | ✅ |
| 用户列表 / 启停 / 删除 | ❌ | ❌ | ✅ |
| 手动触发访客清理 | ❌ | ❌ | ✅ |
| 平台统计看板 | ❌ | ❌ | ✅ |

---

## 3. 功能需求

> 编号规则：FR-<模块><序号>。标注【V2.2 新增】的为本迭代开发项。

### FR-A 身份与账户

#### FR-A1 登录
- 用户名 + 密码登录；成功后进入工作台，头部显示身份徽标
- **防暴力破解**：同一用户名连续失败 5 次，锁定 10 分钟
- 错误提示统一为"用户名或密码错误"，不暴露账号是否存在

#### FR-A2 注册
- 用户名（唯一）+ 邮箱（唯一）+ 密码
- 密码规则：≥8 位且同时含字母和数字
- **首个注册用户自动成为管理员**
- 注册成功即登录，无需二次登录

#### FR-A3 游客体验
- 免注册一键进入，按客户端 IP 识别身份，同一 IP 未过期访客自动复用
- 数据保留 **24 小时**，到期自动清理（任务、日志、上传与导出文件）
- 防滥用：单访客最多 10 个任务；同一 IP 24h 内最多新建 5 个访客身份，超限提示注册
- 头部显示剩余时长倒计时；到期前可一键转注册（FR-A4）

#### FR-A4 访客转注册
- 访客在有效期内填写用户名/邮箱/密码一键"转正"
- **原访客名下任务与文件完整迁入新账户**，数据不丢

#### FR-A5 修改密码【V2.2 新增】
- 入口：**个人中心（FR-A6）内「修改密码」按钮**
- 表单：旧密码、新密码、确认新密码
- 规则：旧密码校验错误拒绝；新密码与 FR-A2 同强度规则；两次输入需一致
- 成功后当前登录态失效，引导重新登录（保证新密码即刻生效的确定性）
- 访客不提供该入口（访客无密码，转注册即设密码）

#### FR-A6 个人中心【V2.2 新增】
- 入口：登录后点击头部身份徽标（用户名 chip）
- 展示当前账户信息：
  - 注册用户：用户名、角色（管理员/普通用户）、邮箱
  - 访客：访客身份、数据剩余保留时长、转注册引导
- 内含操作：修改密码（FR-A5，访客隐藏）、用户管理（FR-F，仅管理员）、切换账户（FR-A7）、退出登录（FR-A8）
- 弹窗交互：✕ 按钮 / 点击空白 / ESC 均可关闭

#### FR-A7 切换账户【V2.2 新增】
- 入口：个人中心内「切换账户」
- 行为：清除当前登录态并回到登录弹窗，可直接登录另一账户，无需二次点击
- 当前账户数据不受影响（任务/文件按账户隔离保留）

#### FR-A8 退出登录
- 入口：头部「退出」按钮及个人中心内「退出登录」
- 清除本地登录态，回到未登录视图

### FR-B 任务提交

- **两种输入源**：上传规格文件（.md/.txt 等）或直接粘贴需求文本
- **两种任务类型**：接口规格（API）/ 业务需求（business），决定用例生成策略
- **导出格式多选**：xlsx / json / xmind，任务提交前勾选
- 未登录提交 → 自动弹出认证弹窗
- 任务提交后进入工作流队列，前端实时刷新状态

### FR-C 工作流执行（四 Agent 编排）

| 步骤 | Agent | 职责 | 失败处理 |
|---|---|---|---|
| 1 解析 | Parser | 提取接口/需求要素，输出结构化中间表示 | 任务置 failed，展示错误步骤 |
| 2 生成 | Generator | 按策略生成用例集（正向/异常/边界） | 同上 |
| 3 评审 | Reviewer | 校验用例质量（覆盖度/规范性），输出质量报告 | 同上 |
| 4 导出 | Exporter | 按勾选格式生成下载文件 | 同上 |

- 步骤执行实时记录日志（时间、状态、摘要），前端时间线可视化
- 大模型未配置 API Key 时自动 **Mock 兜底**：流程、产物、报告完整，仅内容由规则生成器产出——保证零成本演示

### FR-D 任务管理与查看

- 任务列表：实时轮询刷新，展示名称/类型/状态/创建时间
- 任务详情：四步骤时间线 + 每步日志 + 质量报告
- **数据隔离**：普通用户只见自己的任务；管理员可查看全部（`?all=true`）；越权访问返回 404 防枚举

### FR-E 导出与下载

- 支持格式：xlsx（测试管理工具可导入）、json（自动化框架可用）、xmind（脑图评审）
- 文件按任务维度存放，按用户目录隔离；下载需鉴权，禁止跨用户下载
- xmind 采用测试管理通用层级：模块 → 用例（序号.操作→预期）→ 前置/数据/步骤，类型与优先级以标签呈现

### FR-F 管理后台（admin）

- 入口：个人中心内「用户管理」按钮（仅管理员可见）
- **统计看板**：注册用户数、活跃访客数、24h 清理数、任务总数
- **账户列表**：ID、用户名、角色、状态、任务数、注册时间
  - 禁用/启用：**禁用后下一请求即生效**（每请求回查用户状态）
  - 禁用访客 = 立即清理其全部数据（任务/日志/文件）
  - 删除：二次确认后级联删除任务、日志与文件目录，不可恢复
  - 管理员本人行显示"当前账户"，不可自禁自删
- **访客治理**（独立区块，与注册用户分开展示）：
  - 活跃访客列表：访客身份、剩余时长、任务数
  - **定向清理**：单行「清理」按钮，随时可清任意访客（无论是否到期）
  - **清空全部访客**：一键批量清理所有活跃访客（二次确认，立即删除其任务/日志/文件）
  - 清理过期访客：例行维护按钮，只清理已到期的
- 24h TTL 自动清理始终兜底，手动清理仅作主动治理手段

### FR-G 安全与流量加密

- 认证：JWT（HS256，24h），每请求回查用户状态——禁用/删除即时生效
- **分级流量加密**：管理员明文（便于调试）；普通用户与访客的 API 流量 AES-256-GCM 强制加密，抓包不可见业务数据结构；登录/注册/访客签发为密钥分发通道（明文下发会话密钥）
- 文件上传/下载保持二进制直传（不加密）
- 密码存储：bcrypt 哈希；全链路无明文密码落库

---

## 4. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 单任务（百条用例级）全流程 < 1 分钟（mock 模式 < 3 秒）；页面轮询间隔 4s |
| 安全 | 见 FR-G；接口越权统一 404；登录限速；访客 TTL 自动清理 |
| 兼容 | 主流现代浏览器（Chrome/Edge/Safari）；HTTP 非安全上下文可用（加密为纯 JS 实现，不依赖 crypto.subtle） |
| 可演示性 | 零外部依赖即可完整演示（mock 兜底）；5 分钟内可从启动到导出全流程走通 |
| 可维护 | 前端单文件无构建；后端 FastAPI 分层；测试用例集（pytest 68 条）随代码演进回归 |

---

## 5. 版本规划

| 版本 | 内容 | 状态 |
|---|---|---|
| V1.0 (MVP) | 四 Agent 工作流 + 任务管理 + 三格式导出 + mock 兜底 | ✅ 已上线 |
| V2.0 | 三级角色认证体系：注册/登录/访客生命周期/admin 治理 + 数据隔离 | ✅ 已上线 |
| V2.1 | API 分级流量加密（AES-256-GCM）+ 前端登录/注册/游客三入口 | ✅ 已上线 |
| **V2.2** | **个人中心（FR-A6）+ 修改密码（FR-A5）+ 切换账户（FR-A7）+ PRD 文档独立** | 🚧 开发中 |
| V2.3（规划） | React + TS 前端重写（当前为原生 HTML 过渡版） | 📋 规划 |
| V2.4（规划） | 定时执行 + Allure 报告 + 部署阿里云在线演示 | 📋 规划 |

---

## 6. 名词解释

| 名词 | 说明 |
|---|---|
| 四 Agent | 平台工作流的四个智能体环节：解析 → 生成 → 评审 → 导出 |
| Mock 兜底 | 未配置大模型 Key 时，由规则生成器替代 LLM 产出，保证流程完整 |
| 访客 TTL | 访客身份及其数据的有效期（24h），到期自动清理 |
| 分级加密 | 按角色决定 API 流量是否加密：admin 明文 / user+guest 强制 AES-256-GCM |
| 防枚举 | 接口对"资源不存在"与"无权限"统一返回 404，避免探测他人资源是否存在 |

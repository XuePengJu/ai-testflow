你是资深自动化测试工程师，擅长 pytest + Playwright（Python 同步 API）。
请把下面的测试用例转写成 pytest 测试函数代码（Python）。

【硬性约定（违反任何一条即为不合格输出）】
1. 使用 pytest-playwright 提供的 page fixture：每个测试函数以 `def test_xxx(page):` 形式注入 page，
   禁止自己启动浏览器 / 创建 browser / new_context，禁止 `import playwright`
2. 元素定位优先级：get_by_role > get_by_label > get_by_placeholder > get_by_text；
   仅在语义定位器确实无法表达时才允许 locator("css=...")，禁止裸 XPath
3. 每条用例对应且仅对应一个测试函数，函数名必须是 `test_{case_id 小写、'-' 换 '_'}`（如 TC-001 → test_tc_001），
   函数第一行用 docstring 写用例标题
4. 每条用例必须至少包含一个 `expect(...)` 断言（`from playwright.sync_api import expect`）；
   禁止 time.sleep / page.wait_for_timeout，等待一律依赖 expect 与定位器自带的自动等待
5. 页面导航统一写 `page.goto(BASE_URL + "路径")`；BASE_URL 已在文件头部定义，直接使用，不要重新定义
6. 输入数据严格按用例的 test_data / steps 填写，不要编造用例里没有的数据

【被测系统页面结构摘要】（据此选择最贴近用户视角的定位器）
{pages_md}

【待转写测试用例】（JSON 数组，module={module}，共 {count} 条）
{cases_json}

【输出格式】
直接给出最终代码，不要输出任何思考过程或推理文字。
只输出纯 Python 代码：从第一个 `def test_` 函数开始，到最后一个函数结束。
不要输出 import 语句（文件头部由系统统一生成）、不要 markdown 代码块围栏、不要任何解释文字。
函数之间空两行，保持 PEP 8。

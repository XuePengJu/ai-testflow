# 项目1 - 模型池与思考控制执行方案 V1.0

> 状态：**待老板确认后开工**（本方案只做设计，未改任何生产代码）
> 日期：2026-09-24
> 关联：PRD FR-I（模型接入配置）、FR-O（深度思考管道）、FR-AC/AD（V5.0 全链路）
> 前序：`.workbuddy/memory/2026-09-24.md`（e2e 耗时排查 + D 方案解析健壮化）

---

## 0. 本方案要解决的两个问题

| # | 问题 | 现象 | 本方案对应章节 |
|---|---|---|---|
| P1 | **生成链路思考全开，耗时离谱** | e2e 任务 44 分钟：生成用例 704s + 脚本生成 1876s | §1–§3 |
| P2 | **单模型无冗余，撞限流即任务失败/降级** | 用户只配一个模型，429/额度尽只能等 | §4 |

两件事同源：**都在"每次 LLM 调用"这一层**。P1 是"单次调用太贵"，P2 是"单次调用失败没有退路"。

---

## 1. 实测：思考开关的真实代价与收益

### 1.1 方法

用项目 `.venv` 直连生产解析链（`resolve_effective` → `OpenAICompatClient._build_model`），
**输入全部取自真实任务 `b6d292a4903c` 的产物**（`uploads/u_1/b6d292a4903c.pages.md` + 38 条真实用例）。
只读探针，不写业务库。

- 模型：`Qwen/Qwen3.8-Flash-Next`（platform 默认，ModelScope 免费端点）
- 开关注入：`_build_model(..., enable_thinking=flag)`。**注意 `chat()` 目前不支持该参数**（见 §3.1）
- 思考字数取 `additional_kwargs.reasoning_content` 累加；正文字数取 `chunk.content` 累加

### 1.2 实验 A：脚本生成（8 条用例 → Playwright 代码）

| 档位 | 耗时 | 思考字数 | 正文字数 | 语法校验 | test_ 函数 |
|---|---|---|---|---|---|
| 关思考 + max_tokens 8192 | **61.4s** | 0 | 9847 | ✅ 通过 | 8 |
| 关思考 + max_tokens 32768 | **55.3s** | 0 | 10034 | ✅ 通过 | 8 |
| 开思考 + max_tokens 32768（**生产现状**） | **392.1s** | 72565 | 11418 | ✅ 通过 | 8 |

**结论**：关思考提速 **6.4~7.1 倍**；产出质量（语法合法性、函数数量）**完全一致**。
思考写了 7.2 万字，正文只比关思考多 1384 字 —— **思考基本是纯开销**。
生产 DB 记录的 375.3s/批与此处 392.1s 吻合，探针可信。

### 1.3 实验 B：探索 Agent 单步决策（max_tokens=2048，生产值）

| 档位 | 耗时 | 思考字数 | 正文字数 | 决策解析 |
|---|---|---|---|---|
| 关思考 | 1.9s | 0 | 155 | ✅ `browser_navigate` |
| 开思考 | 3.9s | 244 | 76 | ✅ `browser_snapshot` |

**结论**：这处差异很小（思考仅 244 字）。**原先"2048 预算会被思考挤爆"的担心未复现**。
两档决策都合理（一个直接跳转、一个先取快照）。**此处关/开皆可**，收益不显著。

### 1.4 实验 C/D：测试用例生成（3 个测试点 × 4 档位）

数据取自上一轮 44 分钟任务里**真实出现过 0 条的**那几个测试点。

| 测试点 | 关思考+8192 | 开思考+8192（**生产**） | 开思考+32768 | 关思考+2048 |
|---|---|---|---|---|
| 系统设置表单保存校验 | **8 条** / 28.9s | **1 条** / 113.7s | — | — |
| 采购订单创建入口验证 | 6 条 / 25.3s | 8 条 / 61.1s | 8 条 / 119.8s | 6 条 / 21.4s |
| 进销存核心模块入口验证 | **8 条** / 20.5s | **1 条** / 114.7s | 7 条 / 84.5s | 7 条 / 29.3s |
| **均值** | **7.3 条 / 24.9s** | **3.3 条 / 96.5s** | 7.5 条 / 102.2s | 6.5 条 / 25.4s |

**结论（最关键的一条）**：

> **开思考 + 8192（= 当前生产配置）是三个档位里产出最少、最不稳定的那个** ——
> 3 个测试点里有 **2 个只输出 1 条用例**，且耗时是关思考的 **3.9 倍**。

**机制推断**：开思考时模型把分析放进 reasoning，正文倾向于输出"摘要式"的少量结果
（不是 token 物理截断 —— 开思考的正文长度 3897/4075 字反而**比关思考的 2589/3496 字更长**）。
对"要求枚举 N 条结构化用例"的任务，这是灾难性的行为偏移。

**这直接解释了上一轮 44 分钟任务里 `12/8/0/0/7/8/0` 的产出形状**：
不是模型答不出，也不是纯解析失败，而是**开思考让部分测试点的正文退化成 0~1 条**。

### 1.5 三条硬结论

| # | 结论 | 证据 |
|---|---|---|
| 1 | **关思考快 4~7 倍** | 用例生成 24.9s vs 96.5s；脚本生成 55s vs 392s |
| 2 | **关思考产出更稳** | 关思考 5 次全部 6~8 条；开思考+8192 三次里两次仅 1 条 |
| 3 | **思考在本项目里几乎不创造价值** | 脚本生成思考 7.2 万字 → 正文只多 1384 字；产出结构完全一致 |

> ⚠️ **纠正上一轮的一处错误结论**：上一轮我据单次采样说"关思考代价是用例数 -25%（8→6 条）"。
> 本次 5 次关思考 + 3 次开思考的对照推翻了它 —— **真实情况是关思考产出更多、更稳定**。
> 那个 -25% 是采样噪声。

---

## 2. 关思考决策表（全部 11 处 LLM 调用点）

扫描范围：`app/` + `generator_core/` 全量 `.py`。除对话链路外，**没有任何一处传过思考参数**。

| # | 调用点 | 任务性质 | 输出形态 | 当前 max_tokens | 建议 | 依据 |
|---|---|---|---|---|---|---|
| 1 | `case_generator` → `injected.generate()` | 需求点 → 用例 | JSON 数组 | 8192 | **关**（并降至 4096） | §1.4 实测 |
| 2 | `parser_agent.py:138` `client.generate()` | 文本 → 测试点 | JSON | 8192 | **关** | 同源结构抽取 |
| 3 | `supplement_agent.py:209` `client.generate()` | 补/迭代用例 | JSON | 8192 | **关** | 同源 |
| 4 | `reviewer_agent.py:143` `client.chat()` | AI 补维度用例 | JSON | **2048** | **关（必）** | 预算小，开思考必受挤压 |
| 5 | `scripter_agent.py:205` `client.chat()` | 用例 → Playwright 代码 | 纯代码 | 32768 | **关**（并降至 8192） | §1.2 实测，提速 7 倍 |
| 6 | `auto_healer.py:187` `llm_client.chat()` | 失败日志 → 根因 + 完整修复文件 | JSON（含长代码） | 8192 | **关（必）** | 输出含完整文件，预算紧张 |
| 7 | `explorer_agent.py:586` `llm_client.chat()` | 页面快照 → 下一步动作 | JSON | 2048 | **关**（收益小，为一致性） | §1.3 实测差异小 |
| 8 | `api/knowledge.py:715`（裸 `AsyncOpenAI`） | 文档 → Wiki 摘要 | JSON | **400** | **关（必）** | 400 预算，思考必挤爆 |
| 9 | `llm_service.chat_stream()` | 对话 / 知识库问答 | 自由文本 | 8192 | **保持用户开关（默认开）** | 唯一真正的开放式任务 |
| 10 | `api/llm_config.py:330` 连通测试 | 探活 | 短文本 | 2048 | **关** | 只测可达与延迟 |
| 11 | `llm_service.test_connectivity()` | 探活 | 短文本 | **16** | **关（必）** | 16 预算，思考必挤爆 |
| — | `describe_image()`（vision 槽） | 截图理解 | 文本 | 1024 | **不动**（显式 `None`） | 视觉模型一般无 thinking |

### 「哪里必须开着」的答案

**只有一处：对话助手（#9）**，而且保留用户自己的「💡深度思考」开关，不改默认值。
理由：它是唯一"开放式问答 + 用户主动想看到推理过程"的场景，属于产品体验，不是批量吞吐。

**其余 10 处全关。** 它们清一色是"给定输入 → 按固定格式输出"，思考不产生增量，只产生延迟与不稳定性。

**边界说明**：当前没有任何"无固定输出格式的多步推理"类调用。若未来引入（如跨任务根因分析、
测试策略规划），需**单独评估**是否开思考，不能照搬本表。

---

## 3. 关思考的实现方案

### 3.1 现状约束（重要，决定改法）

```python
# app/services/langchain_client.py
def chat(self, messages, temperature=0.3, max_tokens=8192, timeout=_TIMEOUT) -> str:
    for chunk in self._build_model(temperature, max_tokens, timeout).stream(messages):
        #                                       ↑ 第 4 参是 enable_thinking，但没接
```

- `chat()` / `generate()` **根本没有 `enable_thinking` 形参** → 调用点"想传也传不了"
- 只有 `chat_stream()` 支持（对话链路专用）
- `_build_model()` 本身支持，且有「端点不认该参数 → 400 自动去掉重试一次 + 记入 `_NO_THINKING_PARAM`」的兼容层
- 两套实现都要改：`LangChainClient`（默认后端）与 `_HttpxCompatClient`（`AITF_LLM_BACKEND=httpx` 应急）

### 3.2 两种改法

| 方案 | 做法 | 优点 | 代价 |
|---|---|---|---|
| **A（推荐）默认关** | `chat()`/`generate()` 新增 `enable_thinking: bool \| None = False`，默认即关；`describe_image` 显式传 `None`；对话走 `chat_stream` 不受影响 | **调用点零改动**，不可能漏改；语义正确（`chat()` 的消费方全是结构化任务） | 隐式默认变更，需在 docstring 与 PRD 里写明 |
| B 逐个显式传 | 默认保持 `None`，§2 表里 10 处调用点逐个传 `False` | 改动显式、可审计 | 10 处 × 2 类调用（`chat`/`generate`）= 易漏改；新增调用点仍会忘 |

**推荐 A**。理由：本项目里 `chat()`/`generate()` 的**全部消费方**都是结构化输出任务，
"默认不思考"才是正确默认值；而唯一需要思考的对话链路走的是另一个方法，天然隔离。

### 3.3 改动清单（方案 A）

| 文件 | 改动 | 说明 |
|---|---|---|
| `app/services/langchain_client.py` | `LangChainClient.chat()` 加参数并透传给 `_build_model`；`generate()` 同步透传 | 主实现 |
| 同上 | `_HttpxCompatClient.chat()` / `generate()` 同步 | 应急后端保持一致 |
| 同上 | `describe_image()` 显式 `enable_thinking=None` | 视觉槽保持"不注入" |
| `app/services/llm_service.py` | `test_connectivity()` 显式 `enable_thinking=False` | 探活不该花思考的钱 |
| `app/api/knowledge.py` | 裸 `AsyncOpenAI` 调用加 `extra_body={"enable_thinking": False}` | 该处未走统一客户端 |
| `app/workflow/agents/scripter_agent.py` | `_call_llm` 的 `max_tokens` 32768 → **8192** | 关思考后 8192 已足够（实测 9847 字通过） |
| `generator_core/.../case_generator.py` | 用例生成 `max_tokens` 8192 → **4096**（可选） | 实测 2048 也能出 6~7 条 |
| `tests/*` | 若干 fake client 的 `generate(prompt)` 签名 | 默认参数变更后需对齐 |

**预计收益（叠加现有 e2e 链路）**：

| 步骤 | 现状 | 关思考后（预估） |
|---|---|---|
| AI 生成用例（7 测试点） | 704s | **~175s** |
| 脚本生成（5 批） | 1877s | **~275s** |
| **任务总计** | **44 分钟** | **~10 分钟** |

> 公式：用例生成 7×24.9s≈175s；脚本生成 5×55.3s≈277s；其余步骤（抓取/解析/校验/导出）不变约 76s。

---

## 4. 多模型池设计（A 方案 · 按老板拍板细化）

### 4.1 拍板记录

| 决策项 | 结论 |
|---|---|
| 池化范围 | **不做优先级区分**，三个槽各自成池：`text` / `vision` / `embedding`，**每槽可加多条** |
| 数据模型 | **方案 A：新增 `llm_model_pool` 表**，`llm_configs` 一行不动 |
| 付费模型 | **允许**。池内模型一律使用**用户自己配置的 API Key**，费用由使用者承担；平台不代付 |
| 思考开关 | 另行评估（= 本方案 §1–§3） |

### 4.2 数据模型：新增 `llm_model_pool`

不做 DDL 改老表的依据：`init_db()` 走 `Base.metadata.create_all()`，**新表零迁移成本**；
且 `llm_configs` 上现有的 `scalar_one_or_none()` 查询（`resolve_embedding` 等）在同一 `(user_id, slot)`
出现多行时会抛 `MultipleResultsFound`，改它牵连面大。（方案 B/C 已在上轮评估中否决）

```python
class LLMModelPool(Base):
    __tablename__ = "llm_model_pool"
    __table_args__ = (
        # 重复配置校验：base_url + model + Key 指纹（+ 归属 + 槽位）全同才算重复
        UniqueConstraint("user_id", "slot", "base_url", "model", "key_fingerprint",
                         name="uq_pool_dedup"),
    )

    id              = Integer, primary_key, autoincrement
    user_id         = Integer, index       # 0 = 平台默认池（与 llm_configs 同约定）
    slot            = String(16)           # text / vision / embedding
    provider        = String(32)           # 仅展示用预设标签
    base_url        = String(256)
    model           = String(128)
    api_key_enc     = Text                 # AES-256-GCM（复用 encrypt_key / _at_rest_key）
    api_key_tail    = String(16)           # 仅脱敏展示
    key_fingerprint = String(32)           # sha256(api_key)[:16] —— 判重专用
    priority        = Integer, default 100 # 小 = 优先；同级按 id 顺序（轮询指针见 §4.5）
    enabled         = Boolean, default True
    paid            = Boolean, default False  # 仅 UI 标注"付费模型"，不参与调度过滤
    note            = String(64)           # 备注，如「备用-免费」

    # —— 运行时健康状态（放表里，避免多 worker / 重启丢失）——
    cooldown_until  = DateTime, nullable   # 熔断到期时间；未到期直接跳过
    last_error      = Text, nullable
    last_error_at   = DateTime, nullable
    success_count   = Integer, default 0
    fail_count      = Integer, default 0

    created_at / updated_at
```

**`key_fingerprint` 的必要性**：`crypto.encrypt_obj()` 用随机 nonce，**同一 Key 加密两次结果不同**
（已实测 `a == b → False`）。所以唯一约束**不能**用 `api_key_enc`，必须引入确定性指纹列。

### 4.3 生效解析与回落（零破坏）

新增 `llm_service.resolve_pool(db, user, slot) -> list[dict]`，返回**候选列表**（按 priority, id 排序）：

```
1. 用户池（user_id = user.id, slot, enabled=True）非空  → 用它
2. admin 平台池（user_id = 0, slot, enabled=True）非空   → 用它
3. 两边池都空                                            → 回落现有 resolve_effective 单条逻辑
```

`resolve_effective()` / `resolve_embedding()` **签名与返回值保持不变**（仍返回单条），
池化只新增入口。这样：
- 池空时行为与今天**完全一致**，不会因上线池化而改变任何现有任务的行为
- 存量用户的 `llm_configs` 配置继续生效，无需数据迁移

### 4.4 PoolClient 设计

新增 `app/services/llm_pool.py`，提供**接口与 `OpenAICompatClient` 完全一致**的包装类：

```python
class PoolClient:
    """多模型池客户端：接口与 OpenAICompatClient 一致（chat/generate/chat_stream/describe_image）。
    内部按候选顺序尝试；遇到「可切换错误」换下一个，全池耗尽才抛 LLMError。"""
    def __init__(self, candidates: list[dict], slot: str):
        ...
    def chat(self, messages, temperature=0.3, max_tokens=8192,
             timeout=_TIMEOUT, enable_thinking=False) -> str: ...
    def generate(self, prompt) -> str: ...
    def chat_stream(self, messages, **kw): ...        # 打字机协议不变
    def describe_image(self, image_url, hint="") -> str: ...
```

配套工厂：`build_client(db, user, slot) -> OpenAICompatClient | PoolClient`

**调用点改造**（各 1~2 行，把 `OpenAICompatClient(...)` 换成 `build_client(...)`）：

| 调用点 | 备注 |
|---|---|
| `app/workflow/engine.py:139`（text）/ `:157`（vision） | 主工作流，收益最大 |
| `app/workflow/iterate.py:154` | 迭代任务 |
| `app/workflow/agents/scripter_agent.py:156` | 脚本生成 |
| `app/services/auto_runner.py:54` | 自愈 |
| `app/services/llm_service.py:415` | 对话链路（流式） |
| `app/api/llm_config.py:329` | 连通测试 |
| `app/api/knowledge.py:699` | ⚠️ 裸 `AsyncOpenAI`，需单独适配或改用 `build_client` |

### 4.5 切换与熔断策略（方案核心，不是「失败就无脑换」）

复用已有的错误归类逻辑 —— 但需**上移**：`_classify_error()` 目前写在 `app/api/llm_config.py:187`（API 层），
池化在 service 层要用，应迁到 `app/services/llm_pool.py`（API 层改为 import），避免反向依赖。

| 错误类型 | 动作 | 理由 |
|---|---|---|
| `rate_limit`(429) / `quota`(402 额度尽) / `server`(5xx) / `network`(超时) | **切下一个**，并给该条记 `cooldown_until = now + N 分钟` | 换服务商就能继续；免费额度是"按日耗尽"的长期状态，不熔断会每次白撞 |
| `auth`(401/403) / 参数错(400) / 模型不存在 | 标记该条不可用，**本轮所有候选一起跳过** | 否则会在每个模型上各撞一次，白花数倍时间 |
| **调用成功但解析出 0 条**（质量类） | **不切换** | 语义是"模型答不出"而非"调用失败"；换一个大概率同样答不出。改由 §1 的关思考 + D 方案的失败上报处理 |

**熔断参数建议**：`rate_limit` 冷却 5 分钟；`quota` 冷却至次日 00:05（按日额度）；`server/network` 冷却 1 分钟。
冷却期内该条直接从候选里过滤；全池都在冷却 → 取冷却最早到期的一条"破格尝试"（避免无限等待）。

**轮询与顺序**：`priority` 升序；同 `priority` 内按 `id`。不做随机化 —— 用户按自己的额度预期排序。

### 4.6 三个必须提前知道的坑

| # | 坑 | 对策 |
|---|---|---|
| 1 | **流式只能在「首字节前」切换** —— 已吐字再换会出现两个模型文本混排 | 沿用现有 `chat_stream` 的 `produced` 标记：仅在未产出任何 delta 时允许换候选 |
| 2 | **超时不能立刻换**：3 个模型各 180s 超时 = 9 分钟白等 | 单模型短超时（建议 60s）+ 整次调用总预算（如 180s），两者同时约束 |
| 3 | **池内混入付费模型 = 静默产生费用** | 老板已确认「费用用户承担」，但 UI 必须**明确标注「付费」徽标** + 池列表显示本次切换留痕（哪个模型失败、切到了谁） |

### 4.7 API 清单（新增）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/llm/pool/{slot}` | 列出当前用户该槽的池（按 priority 排序，含健康状态） |
| POST | `/llm/pool/{slot}` | 新增一条（返回 409 = 重复配置） |
| PUT | `/llm/pool/{slot}/{id}` | 修改（`api_key` 留空 = 保留原 Key，与现有约定一致） |
| DELETE | `/llm/pool/{slot}/{id}` | 删除 |
| POST | `/llm/pool/{slot}/reorder` | 调整优先级（传 id 顺序数组） |
| PATCH | `/llm/pool/{slot}/{id}/enabled` | 单条启停 |
| POST | `/llm/pool/{slot}/{id}/test` | 单条连通测试（不落库） |
| GET | `/llm/pool/{slot}/health` | 查看各条冷却/成功率（可选） |

平台默认池（`user_id=0`）同名加 `/llm/platform-pool/{slot}` 前缀，仅 admin 可操作。

### 4.8 前端改造（本方案最大工作量）

现状：`frontend/src/components/settings/LLMConfigCard.tsx`（248 行）是**单条表单**（slot × mode 复用）。

目标形态：**列表 + 抽屉/行内编辑**

- 每个 slot 一张卡：「模型池」标题 + 条数徽标
- 列表行：`优先级拖拽手柄 | 厂商 | 模型名 | Key 脱敏 | [付费] [启用] | 连通状态灯 | 编辑/删除`
- 行内操作：单条「测试」、单条启停、拖拽排序
- 新增/编辑走同一个表单（沿用现有厂商预设 + Base URL 自动带出 + datalist 模型建议）
- 重复配置：前端预校验 + 后端 409 双保险，报错文案明确指出"与第 N 条重复"
- 切换留痕：任务详情/步骤详情里显示"本次调用由 A 切换到 B（原因：限流）"
- 池为空时**保持现有单条表单**（避免功能倒退）

---

## 5. 分期计划

| 阶段 | 内容 | 影响面 | 前置 |
|---|---|---|---|
| **P0 · 关思考** | §3 改动清单全部 | 8 个文件（多为 1~3 行）+ 测试对齐 | 老板确认 §3.2 选 A |
| **P1 · 池化后端** | 池表 + `resolve_pool` + `PoolClient` + 熔断 + 判重 + 7 个 API；接入 text 槽 4~5 个调用点 | 新增 2 文件 + 改 6 文件 | P0 完成（避免两处同时改客户端） |
| **P2 · 前端池管理** | §4.8 列表化改造 | `LLMConfigCard.tsx` 重写 + 新增池 API 封装 | P1 接口冻结 |
| **P3 · 扩展（可选）** | vision / embedding 槽接入池；后台健康巡检；失败率自动调序 | — | P2 |

**建议 P0 先做**，理由：① 改动小、风险低、收益最大（44min → ~10min）；
② **思考让每次请求的 token 翻数倍，本身就在推高撞限流的概率** —— 关思考是**降低 P2 发生频率**的治本动作，
池化只是容错。顺序反了会先花大力气建容错、却不去掉主要的失败诱因。

---

## 6. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 默认关思考后，个别模型不认 `enable_thinking` 参数报 400 | 低 | 现有 400 自动去掉参数重试 + 记忆端点机制已覆盖；补一条回归测试 |
| `chat()` 签名变更影响测试 fake client | 低 | 测试里的 fake 都是 `def chat(self, messages, **kwargs)`，天然兼容；仅个别 `generate(prompt)` 需加默认参数 |
| 关思考后个别模型产出反而变少 | 中 | 实测本模型是相反方向（关思考更多）；上线后观察 1~2 个真实任务，若有反例则该模型单独配置 |
| 池化后付费模型被静默调用 | 中 | UI 付费徽标 + 切换留痕；可选「仅免费候选」开关（老板已确认整体允许付费，此开关作为可选增强） |
| 熔断状态并发写冲突 | 低 | uvicorn 单进程；写库用 `db.commit()` 短事务，失败不阻断主调用 |
| 新表与老表配置不一致（用户同时有两处配置） | 中 | 明确优先级：**池非空则池优先，池空回落单条**；UI 上写明"配置池后，单条配置仅在池为空时生效" |

---

## 7. 待确认事项

1. ✅ **已定：默认关（方案 A）** —— 落地时收敛为「对话按需自动判定」，见 §8。
2. ✅ **P0 已做（2026-09-24）**，含实测验证与回归测试，见 §8。
3. **§4.5 冷却时长参数**（rate_limit 5min / quota 至次日 / server 1min）是否接受？
4. **探索 Agent（#7）关不关**？（实测差异小，倾向"为一致性一起关"）
5. 其余 10 处结构化调用点按 §3 关思考，是否现在做？

---

## 8. 落地记录（2026-09-24）· 对话按需思考（原「方案 C」）

### 8.1 与 §3 原方案的差异（重要）

§3 设想的是「全局默认关思考」。落地时因为对话是唯一需要**保留**推理能力的调用点，
收敛为更精确的形态：**对话默认「按需自动判定」**；其余 10 处结构化调用点仍按 §3 关闭（待做）。

| 项 | §3 原设想 | 实际落地 |
|---|---|---|
| 对话链路 | 默认关（要深度分析得手动开） | **默认按需自动判定**：简单问题直接答，复杂问题自动推理 |
| 前端开关 | 保留「深度思考」两态 | 改名「**总是深度思考**」：关=按需自动，开=每轮都推理 |
| 判定位置 | 未设计 | **后端**（前端无测试框架，规则放前端不可测） |

### 8.2 改动清单（全部已落盘）

| 文件 | 改动 |
|---|---|
| `app/services/llm_service.py` | 新增 `should_deep_think()`；`chat_stream(enable_thinking: bool\|None = None)`，`None` 时走判定 |
| `app/schemas/llm_config.py` | `ChatIn.thinking: bool\|None = None`（原 `bool = True`） |
| `frontend/src/store/chatStore.ts` | `thinking: draft.thinking ?? null`（**原 `!== false` 会把 null 转成 true，方案会整体失效**） |
| `frontend/src/components/chat/ChatPanel.tsx` | 开关改名「总是深度思考」、默认关、三态透传 |
| `frontend/src/types.ts` | `ChatDraft.thinking?: boolean \| null` |
| `tests/test_deep_think.py` | **新增** 29 个用例（判定边界 + 三态生效 + 2 个防复发回归） |

### 8.3 判定规则（`should_deep_think`）

按序判定，任一命中即开：`kb_mode`（恒关，优先级最高）→ 用户上传附件 → 正文 ≥60 字 →
意图关键词（为什么/排查/分析/对比/评估/根因/方案/架构/设计…）→ 错误特征（Traceback/Error/报错/失败/超时/500/502/404）。

**刻意不收录**「帮我写/帮我改」—— 它们多数走任务生成链路，那条链路本身就关思考。

### 8.4 实测（真实模型 + HTTP 真实 SSE 链路）

| 场景 | 前端传值 | think 事件 | 首字节 | 总耗时 |
|---|---|---|---|---|
| 「你好」 | `null` | **0 个** | 1.43s | 7.8s |
| 「为什么采购订单创建后库存没有增加？」 | `null` | 217 个 | 0.28s | 43.3s |
| 「你好」 | `true`（总是深度思考） | 80 个 | 0.38s | 12.4s |

改造前：「你好」也要等十几秒才出首字节（默认全开）。

### 8.5 踩坑记录（已修 + 已加回归测试）

**`attached_text` 不能当作「用户主动提供上下文」的判据。**
`chat.py` 把它拼成「任务摘要 + RAG 检索结果 + 附件正文」，而 **RAG 默认开启**
（`kb_id=None` 时检索全部可见库）→ 几乎永远非空 → 判定恒为 True。
实测现象：传 `null` 的「你好」开出了 **272 个 think 事件**，方案完全失效。
修正：判据改用 `attach_name`（仅用户主动上传文件时非空），补 2 条防复发回归用例。

### 8.6 验证

- `pytest tests/` → **350 passed / 0 failed**（原 321，本轮 +29）
- 前端 `npm run build` → 通过
- 浏览器截图 → 文案「总是深度思考」、关闭态灰 / 开启态蓝、`aria-pressed` 与 localStorage 均正确、console 0 error
- HTTP 真实 SSE 链路 → 见 §8.4

### 8.7 遗留（老板明确「先留着」）

思考面板当前**默认展开**（`MessageView.tsx` 的 `collapsed` 初始 `false`），大段推理会把答案挤下去。
老板表示该处「逻辑不对了，我在说怎么改」—— 待其给出新逻辑后再动，本轮未改。


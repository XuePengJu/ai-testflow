# PRD：AI 测试工作流平台（ai-testflow）

| 文档信息 | <br />                                            |
| ---- | ------------------------------------------------- |
| 文档版本 | v5.4（功能对齐本地交付现状）                                             |
| 状态   | 线上已发布 V4.5.2（RAG 知识库 / 问答合一 / Embedding 槽 / UI 焕新 / 引用溯源持久化；部署见 https://ai.agentest.vip/）；V5.0–V5.4 本地交付（未部署）：V5.0 全链路闭环 M0–M5（M4 自愈 / M5 探索式 Agent，测试数据隔离待落地）+ V5.1–V5.4 模型接入收敛（多模型池唯一配置入口，单条配置下线）                            |
| 关联文档 | 《项目1-工作流平台-MVP执行方案.md》（技术实现方案，本文档只写"做什么"，不写"怎么做"） |
| 更新日期 | 2026-09-27                                        |

***

## 1. 产品概述

### 1.1 一句话定位

面向软件测试工程师的 **AI 测试闭环平台**（V5.0 由「AI 测试用例生成平台」升级）：以对话驱动的 Buddy 助手为入口，自然语言描述测试需求或上传接口规格/业务需求文档，AI 自动编排解析 → 生成 → 评审 → 导出四个智能体，产出结构化、可下载的测试用例；并打通 **URL → 抓取 → 用例 → 脚本 → 执行 → 报告**的全链路自动化闭环与平台自身质量看板；支持多轮对话上下文、会话持久化回放、多厂商大模型配置、多级分类管理、思维导图在线评审。

### 1.2 产品背景

- 测试用例编写占测试工作量大头，格式 repetitive、易遗漏边界场景

- 现有 LLM 直接对话生成用例：无质量评审环节、无统一格式、不可沉淀复用、对话上下文易丢失

- 本平台将"生成用例"工程化为**对话驱动的可视化工作流**：Buddy 助手自然语言交互 + 每环节独立 Agent + 质量把关 + 标准产出 + 会话持久化可回放

- 支持用户自带大模型 Key，8 家厂商开箱即用，未配模型且关闭演示模式时明确报错（演示模式 `AITF_ALLOW_DEMO=1` 可 mock 兜底）

### 1.3 产品目标

| 目标       | 衡量方式                              |
| -------- | --------------------------------- |
| 用例产出效率提升 | 一份需求描述 → 完整用例集 < 1 分钟（人工需数小时）     |
| 用例质量可控   | Reviewer Agent 评审通过才允许导出，附质量报告    |
| 开箱即用     | 未配置大模型 API Key 时自动 Mock 兜底，全流程可演示 |
| 多用户安全    | 注册/访客双通道 + 数据隔离 + 流量加密            |
| 多模型适配    | 支持主流大厂商 OpenAI 兼容协议，用户级 Key 自管    |
| 用例可评审    | 思维导图在线预览，标签化呈现类型与优先级，便于评审讨论       |
| 对话自然交互   | Buddy 助手多轮对话理解需求，上下文自动关联，会话持久化可回放    |

***

## 2. 用户与角色

### 2.1 角色定义

| 角色            | 来源            | 生命周期            | 定位             |
| ------------- | ------------- | --------------- | -------------- |
| **访客 guest**  | 全站唯一共享账号（`username=guest`，免注册一键共用）【V4.1 简化】 | 不过期、不转正、不限频；数据混用，admin 可一键清空 | 低门槛试用体验 |
| **普通用户 user** | 邮箱注册          | 永久              | 日常使用主力角色       |
| **管理员 admin** | 首个注册用户自动晋升    | 永久              | 平台治理与运营        |

> V4.1 起「按 IP 动态建访客 / 24h TTL / 访客转正」体系已移除，降低滥用面、简化前端登录态同步。

### 2.2 权限矩阵

| 能力                    |    guest   | user | admin |
| --------------------- | :--------: | :--: | :---: |
| 提交任务 / 查看自己的任务 / 导出下载 | ✅（上限 10 个） |   ✅  |   ✅   |
| 任务迭代补充 / 导入用例文件    | ✅（上限内） |   ✅  |   ✅   |
| 任务分类管理（新建/重命名/删除/拖拽）  |      ✅     |   ✅  |   ✅   |
| 模型池配置（用户级，多候选）        |      只读✅   |   ✅  |   ✅   |
| 平台模型池（平台默认）           |      ❌     |   ❌  |   ✅   |
| 查看所有用户任务              |      ❌     |   ❌  |   ✅   |
| 修改自己的密码               |   —（无密码）   |   ✅  |   ✅   |
| 个人中心 / 切换账户           |      ✅     |   ✅  |   ✅   |
| 用户列表 / 启停 / 删除        |      ❌     |   ❌  |   ✅   |
| 清空共享 guest 任务与文件          |      ❌     |   ❌  |   ✅   |
| 查看质量看板（平台自身测试数据）    |      ✅     |   ✅  |   ✅   |
| 运行平台测试（质量采集）          |      ❌     |   ❌  |   ✅   |
| 被测系统管理 / 自动化执行 / 查看报告 |      ✅     |   ✅  |   ✅   |
| 平台统计看板                |      ❌     |   ❌  |   ✅   |

***

## 3. 功能需求

> 编号规则：FR-<模块><序号>。标注【V2.x 新增】的为对应迭代开发项。

### FR-A 身份与账户

#### FR-A1 登录

- 用户名 + 密码登录；成功后进入工作台，头部显示身份徽标

- **防暴力破解**：同一用户名连续失败 5 次，锁定 10 分钟

- 错误提示统一为"用户名或密码错误"，不暴露账号是否存在

#### FR-A2 注册

- 用户名（唯一）+ 邮箱（唯一）+ 密码

- 密码规则：≥8 位且同时含字母和数字

- **首个注册用户自动成为管理员**

- 注册成功即登录，无需二次登录

#### FR-A3 游客体验【V4.1 简化为共享账号】

- 免注册一键进入，全站共用唯一共享账号 `guest`（原「按 IP 动态建访客 / 24h TTL」已移除）

- 共享 guest **不过期、不转正、不限频**；任务上限 10 个，数据与其它访客混用

- 防滥用：共享 guest 的任务与文件由 admin 手动一键清空（`reset_shared_guest_data`），无自动 TTL 清理调度

- 首次进入自动播种示例数据（sample_seeder），开箱即可看到完整功能效果

#### FR-A4 访客转注册【V4.1 起已移除】

- 原「访客有效期内一键转正、任务与文件迁入新账户」能力随动态访客体系一并移除

- 现行行为：直接注册新账号（FR-A2）；共享 guest 数据不随注册迁移

#### FR-A5 修改密码【V2.2 新增】

- 入口：**个人中心（FR-A6）内「修改密码」按钮**

- 表单：旧密码、新密码、确认新密码

- 规则：旧密码校验错误拒绝；新密码与 FR-A2 同强度规则；两次输入需一致

- 成功后当前登录态失效，引导重新登录（保证新密码即刻生效的确定性）

- 访客不提供该入口（共享 guest 无密码）

#### FR-A6 个人中心【V2.2 新增】

- 入口：登录后点击头部身份徽标（用户名 chip）

- 展示当前账户信息：

  - 注册用户：用户名、角色（管理员/普通用户）、邮箱

  - 访客（共享 guest）：访客身份标识与共享账号说明（不过期、不转正）

- 内含操作：修改密码（FR-A5，访客隐藏）、切换账户（FR-A7）、退出登录（FR-A8）；模型配置（FR-I）自 V5.3 起升为独立一级页面（不再挂在个人中心/设置页内），用户管理（FR-F）为独立「用户管理」页（仅管理员）

- 弹窗交互：✕ 按钮 / 单击空白处 / ESC 均可关闭（均为单击触发，无需双击校验）

#### FR-A7 切换账户【V2.2 新增】

- 入口：个人中心内「切换账户」

- 行为：清除当前登录态并回到登录弹窗，可直接登录另一账户，无需二次点击

- 当前账户数据不受影响（任务/文件按账户隔离保留）

#### FR-A8 退出登录

- 入口：头部「退出」按钮及个人中心内「退出登录」

- 清除本地登录态，回到未登录视图

### FR-B 任务提交与对话交互

- **两种交互入口**：
  - **对话驱动（默认）**：首页 Buddy 助手，自然语言描述测试需求，支持多轮对话上下文追问，AI 理解后自动触发生成任务；预设场景卡片一键填充（Web 登录用例 / 接口参数校验 / App 端功能 / 需求文档解析）
  - **传统表单提交**：上传规格文件（.md/.txt/.json 等）或直接粘贴需求文本，选类型 + 导出格式后提交

- **两种任务类型**：接口规格（API）/ 业务需求（business），决定用例生成策略

- **导出格式多选**：xlsx / json / xmind，任务提交前勾选

- 未登录提交 → 自动弹出认证弹窗

- 任务提交后进入工作流队列，前端实时刷新状态（对话流内联展示节点步骤与详情）

- **提交区显示当前生效模型**：如「qwen-plus · 阿里百炼」或「mock 模式（未配置）」

### FR-C 工作流执行（四 Agent 编排）

| 步骤   | Agent     | 职责                     | 失败处理              |
| ---- | --------- | ---------------------- | ----------------- |
| 1 解析 | Parser    | 提取接口/需求要素，输出结构化中间表示    | 任务置 failed，展示错误步骤 |
| 2 生成 | Generator | 按策略生成用例集（正向/异常/边界）     | 同上                |
| 3 评审 | Reviewer  | 校验用例质量（覆盖度/规范性），输出质量报告 | 同上                |
| 4 导出 | Exporter  | 按勾选格式生成下载文件            | 同上                |

- 步骤执行实时记录日志（时间、状态、摘要），前端时间线可视化

- 大模型未配置 API Key 时自动 **Mock 兜底**：流程、产物、报告完整，仅内容由规则生成器产出——保证零成本演示

### FR-D 任务管理与查看

- 任务列表：实时轮询刷新，展示名称/类型/状态/创建时间/所属分类

- **整体布局**：左侧边栏（新对话 + 历史会话列表 + 任务分类树），主区域为对话流或任务详情；对话流内联展示任务节点步骤，点击节点可展开详情

- 任务详情（右侧抽屉，不新开页面）：**3 个 Tab**（见 FR-J 详情页增强）；抽屉头部（标题 + 状态 + Tab 栏）吸顶固定，用例表格 / 思维导图等内容区独立滚动

- **数据隔离**：普通用户只见自己的任务；管理员可查看全部（`?all=true`）；越权访问返回 404 防枚举

### FR-E 导出与下载

- 支持格式：xlsx（测试管理工具可导入）、json（自动化框架可用）、xmind（脑图评审）

- 文件按任务维度存放，按用户目录隔离；下载需鉴权，禁止跨用户下载

- xmind 采用测试管理通用层级：模块 → 用例（序号.操作→预期）→ 前置/数据/步骤，类型与优先级以标签呈现

- 详情页内可直接触发导出（思维导图 Tab 导出 XMind、用例 Tab 导出 Excel）

### FR-F 管理后台（admin）

- 入口：个人中心内「用户管理」按钮（仅管理员可见）

- **统计看板**：注册用户数、活跃访客数、24h 清理数、任务总数

- **账户列表**：ID、用户名、角色、状态、任务数、注册时间

  - 禁用/启用：**禁用后下一请求即生效**（每请求回查用户状态）

  - 禁用访客 = 立即清理其全部数据（任务/日志/文件）

  - 删除：二次确认后级联删除任务、日志与文件目录，不可恢复

  - 管理员本人行显示"当前账户"，不可自禁自删

- **共享 guest 治理**（V4.1 起，独立区块）：

  - **一键清空共享数据**：清空共享 guest 名下全部任务与文件（二次确认，保留账号本身）

  - 历史遗留清理：早期动态访客体系遗留数据的「清理过期访客 / 清空全部访客 / 定向清理」接口保留，仅作迁移遗留清理用

- 无 24h TTL 自动清理调度（V4.1 起随动态访客体系移除）

### FR-G 安全与流量加密

- 认证：JWT（HS256，24h），每请求回查用户状态——禁用/删除即时生效

- **分级流量加密**：管理员明文（便于调试）；普通用户与访客的 API 流量 AES-256-GCM 强制加密，抓包不可见业务数据结构；登录/注册/访客签发为密钥分发通道（明文下发会话密钥）

- 文件上传/下载保持二进制直传（不加密）

- 密码存储：bcrypt 哈希；全链路无明文密码落库

- API Key 存储：AES-256-GCM 加密落库，接口回显脱敏（`sk-****abcd`）

### FR-H 任务分类与拖拽归类【V2.3 新增】

- **多级分类树（左栏）**：任务列表左栏展示分类树（全部任务 / 未分类 / 自定义分类任意层级），每节点实时显示任务数；分类树吸顶跟随滚动

- **分类管理**：新建（可指定父分类形成多级）、重命名、删除（子分类级联删除，其下任务自动回落「未分类」，任务本身不删除）

- **拖拽归类**：任务卡片直接拖到分类节点完成归类；拖回「未分类」即移出分类

- **拖拽移动分类**：分类节点拖到另一分类下成为其子分类，拖拽过程中目标节点高亮；移动含防环校验（不可挂到自己或子孙下）

- **点击过滤**：点击分类节点只显示该分类及全部子孙分类下的任务，可一键回到全部

- **数据隔离**：每个用户独立的分类树（访客亦然），不可见/不可改/不可归他人分类

### FR-I 模型接入配置（多厂商 LLM Provider → 多模型池）【V2.4 上线，V5.1–V5.4 收敛为模型池】

> 解决「平台只能 mock 跑」的问题：让每个用户都能在界面上配置自己的大模型 API Key，真实调用 LLM 生成用例。
>
> 落地形态：**模型配置一级页面（V5.3）** = 顶栏调度摘要（EffectiveBar，各槽池可用/冷却状态）+ 分段 Tab（admin：「我的模型 | 平台默认」）+ 每槽一张模型池卡片；后端 `app/core/providers.py`（预设）+ `app/services/llm_service.py`（OpenAI 兼容 HTTP 直连 + PoolClient 池调度）+ `app/api/llm_pool.py`（池 CRUD/排序/启停/测连通/健康 API）；管线注入点在 `app/workflow/engine.py`。

- **厂商预设（选即用）**：内置主流大厂的 OpenAI 兼容端点，选中预设后**只需填 API Key，无需手动配 Base URL**：

  | 厂商预设       | Base URL（自动带出）                                      | 典型模型                              | 备注                                               |
  | ---------- | --------------------------------------------------- | --------------------------------- | ------------------------------------------------ |
  | 阿里百炼       | `https://dashscope.aliyuncs.com/compatible-mode/v1` | qwen-plus / qwen-max / qwen-turbo | Token 包同端点                                       |
  | 智谱 GLM     | `https://open.bigmodel.cn/api/paas/v4`              | glm-4.5 / glm-4-flash             | 另含 **Coding Plan 专用端点** `/api/coding/paas/v4` 可选 |
  | 腾讯混元       | `https://api.hunyuan.cloud.tencent.com/v1`          | hunyuan-turbos / hunyuan-lite     | <br />                                           |
  | DeepSeek   | `https://api.deepseek.com/v1`                       | deepseek-chat / deepseek-reasoner | <br />                                           |
  | Kimi（月之暗面） | `https://api.moonshot.cn/v1`                        | kimi-k2 系列                        | <br />                                           |
  | 豆包（火山方舟）   | `https://ark.cn-beijing.volces.com/api/v3`          | 按接入点 ID 填                         | <br />                                           |
  | 自定义        | 用户自填                                                | 任意                                | 任何 OpenAI 兼容端点                                   |

  > 套餐说明：各家的 token 包 / Coding Plan 不改变接入方式——同一套 API、不同计费的 Key；仅智谱 Coding Plan 使用专用端点，已做成预设选项。

- **配置入口（V5.3 一级页面）**：侧栏「模型配置」；**每个用户独立配置**（Key 自管、用量隔离、互不可见）；admin 可额外维护**平台模型池**（对无个人池的用户生效，页内「我的模型 / 平台默认」Tab 切换）；访客只读调度摘要（配置接口 403）。

- **多模型池（V5.1，唯一配置入口）**：`text / vision / embedding` 三槽独立，**每槽可配置多条候选模型**，按优先级从上到下依次调度——某条撞限流 / 额度用尽自动切下一条，任务不中断；支持池内排序（↑↓）、启停开关、单条测连通（V5.4 起统一不带思考）、健康徽标（可用 / 冷却中 / 已停用 / 最近错误）。健康状态随行落库（`cooldown_until` / `success_count` / `fail_count` / `last_error`），重启不丢熔断记忆；同条目按 `(user_id, slot, base_url, model, key_fingerprint)` 唯一判重。**V5.4 起单条配置（每槽仅一条的旧形态）已下线**，存量 `llm_configs` 数据保留但不再参与调度。

- **配置项**：厂商预设 / Base URL（预设自动带出、可改）/ API Key / 模型名（预设建议 + 支持自由填写，如火山方舟接入点 ID）；提供「测试连通」按钮（发一条最小请求验证 Key 有效性，可复用已保存 Key 无需重复粘贴）。

- **Key 安全**：API Key 以 AES-256-GCM 加密落库（复用 FR-G 加密体系）；接口回显一律脱敏（`sk-****abcd`）；日志不落 Key；判重指纹 = sha256(Key) 前 16 位（加密带随机 nonce，不能拿密文判重）。

- **生效优先级（V5.4 纯池解析）**：个人模型池 > 平台模型池 > 服务器环境变量（老部署 `DASHSCOPE_API_KEY` 兼容）> mock 兜底。任务提交区常驻显示当前生效模型（如「qwen-plus · 阿里百炼」或「mock 模式（未配置）」）。

- **协议**：统一走 **OpenAI 兼容协议**（`POST /chat/completions`）；现有 dashscope SDK 原生调用改造为 HTTP 直连（去除 SDK 依赖，一个 client 通吃所有厂商）。Anthropic（Claude 系）messages 协议如需支持另加 adapter，列为可选二期。

- **双模型搭配（文本 + 视觉）**：除「默认模型」外，可再可选配置一个**图像识别模型（多模态）**，专用于理解技术文档 / PRD 中的截图（界面图 / 流程图 / 表格）：

  - **只配默认模型**：全流程都用它；输入含图片时图片被忽略并提示「配置图像识别模型可理解截图」

  - **两个都配**：输入文档含截图时自动两段式——①视觉模型逐图识别，输出结构化文字描述；②描述与文档文本合并后交给默认模型生成用例（推理交给更强的文本模型，视觉模型只负责"看图说话"，控成本）

  - 预设中标注支持图像输入的模型（如 qwen-vl-plus/max、glm-4v 等）；图像走 OpenAI 兼容 `image_url`（base64 data URL）格式

  - **前提**：任务输入支持 Markdown 图片引用解析（已实现：业务需求输入中的 `![](url)` / data URI / 裸贴图片 URL 会被提取，单任务上限 8 张，防跑爆视觉模型）

  - **已实现**：解析前视觉增强——图片引用被视觉模型转成「【截图解读】…」文字块合入文档，再走正常解析与生成；未配置视觉模型时忽略图片并在解析步骤摘要中提示

### FR-J 任务详情页增强【V2.5 新增】

> 任务详情从原来的"四步骤时间线"扩展为三 Tab 布局，强化用例可视化与评审体验。

- **三 Tab 布局**：默认停在思维导图 Tab

  - 🧠 **思维导图 Tab**：MindElixir 在线渲染 xmind 结构，模块 → 用例层级展开，节点支持标签（类型 / P0/P1/P2），原生彩色胶囊样式

  - 📋 **测试用例 Tab**：按模块分组卡片，表格展示完整用例字段（ID / 标题 / 类型 / 优先级 / 前置条件 / 步骤 / 预期结果）

  - ⚙️ **工作流步骤 Tab**：四步时间线，每步状态 / 耗时 / 输出摘要 / 错误信息 / 质量报告

- **思维导图交互**：支持节点展开折叠、拖拽编辑（本地预览）、缩放

- **标签系统**：每个用例节点挂载多个标签（正向用例/异常用例/边界用例 + P0/P1/P2），与 XMind 导出标签一致

- **导出联动**：思维导图 Tab 内可直接导出 XMind 文件；测试用例 Tab 内可直接导出 Excel

### FR-K 产品路线图悬浮抽屉【V2.5 新增】

> 产品包装性功能：页面右侧悬浮"需求进度"入口，展示产品迭代路线与当前进度，增强专业感与期待感。

- **悬浮入口**：页面右边缘垂直排列的"需求进度"标签，hover 显示进度气泡（已上线 X 项）

- **滑出抽屉**：点击悬浮标签从右侧滑出抽屉，覆盖内容区（z-index 高于所有弹窗）

- **三组清单**：

  - **已上线**（绿色标记）：已交付的功能

  - **开发中**（橙色标记）：正在迭代的功能

  - **规划中**（灰色标记）：未来版本计划

- 每条目 hover 显示详细说明（title 属性）

- 数据由前端静态配置，无需后端接口

### FR-L 对话驱动与会话持久化【V2.6 新增】

> 产品核心交互形态升级：从"表单提交任务"升级为"Buddy 对话助手"，用户用自然语言描述测试需求，AI 多轮理解后自动触发生成任务，对话上下文与生成结果持久化可回放。

- **Buddy 对话助手（首页主界面）**：
  - 对话式输入框，支持自然语言描述测试需求（如"帮我生成一个登录接口的测试用例"）
  - AI 回复支持流式输出（SSE），逐字渲染，带思考过程（thinking）可折叠展示
  - 预设场景卡片：Web 登录用例 / 接口参数校验 / App 端功能 / 需求文档解析，点击一键填充并发送
  - 对话内联展示任务生成进度：四 Agent 节点步骤实时刷新，点击节点展开详情（parser/generator/reviewer 输入输出摘要）
  - 任务完成后对话内展示"查看完整用例"按钮，点击在当前页弹出详情抽屉（不新开标签页）

- **多轮对话上下文**：
  - 同一对话内支持多轮追问，AI 理解上下文（如"再加几个异常场景"、"把优先级改成 P0"）
  - 每条 assistant 消息可关联一个生成任务（task_id），聊天流回放时按 task_id 实时拉取任务节点步骤与用例数据，避免冗余存储

- **会话持久化**：
  - 所有对话自动落库（Conversation + Message），刷新页面后历史会话可回放
  - 左侧边栏展示历史会话列表，按时间倒序，点击切换会话
  - 会话标题自动取首条用户消息截断（默认"新会话"）
  - 支持删除会话（二次确认），删除后级联删除该会话下所有消息
  - 数据按用户隔离；共享 guest 的会话由 admin 清空共享数据时一并清理（无 TTL 自动清理）

- **新对话**：左侧边栏「+ 新对话」按钮，一键开启空白对话（不删除历史）

- **对话与任务的关系**：
  - 对话是交互入口，任务是执行单元；一条对话可触发多个任务（多轮生成）
  - 任务详情页可回溯来源对话（通过 conversation_id 关联）
  - 传统表单提交的任务 conversation_id 为空，不关联对话

### FR-M 用例迭代与导入【V2.7 新增】

> 解决"生成后无法再修改"的痛点：已生成用例可在会话中继续补充完善，本地已有用例文件可上传解析后检查完善。每次迭代生成独立版本，历史版本可追溯、可预览、可下载。

- **任务迭代补充**：
  - 已完成任务气泡/详情页提供「➕ 继续补充」入口，点击进入补充模式
  - 补充模式下用户在会话中描述补充范围（如"补充退货审批流程的异常场景"），AI 回复时自动携带当前任务的用例摘要作为上下文
  - AI 确认后点击「✨ 补充生成」，触发迭代流水线：加载基础用例 → 增量生成（不重复已有用例）→ 合并去重 → 质量校验 → 重新导出
  - 每次迭代生成**新版本子任务**（parent_task_id 关联上一版本），不覆盖原任务，命名自动加版本号（v2/v3…）
  - 会话上下文全程保留，无需重复描述需求

- **本地用例文件导入**：
  - 支持上传 xmind / xlsx / json 三种格式的已有用例文件（平台导出格式优先兼容）
  - xmind 兼容 XMind 8 legacy（content.xml）和新版 XMind 2020+（content.json），按"模块→用例→字段"结构还原
  - xlsx 按列名映射（用例ID/标题/模块/类型/优先级/前置条件/步骤/步骤预期/预期结果/测试数据），兼容常见列名别名
  - 导入的用例与原任务用例合并去重，作为迭代的基础用例集
  - 解析失败时明确报错（不静默降级），提示用户使用平台导出格式

- **步骤级预期结果（V2.7 增强）**：
  - 每个操作步骤都有**对应的预期结果**（step_expectations 与 steps 一一对应），xmind/思维导图中每个步骤节点下挂预期结果子节点，不再只挂最后一步
  - LLM 生成 prompt 明确要求逐步输出预期；模型输出缺失/不齐时自动对齐兜底（单步用整体预期、多步每步挂整体预期），保证"步骤必有预期"的硬约束
  - 历史数据无逐步预期字段 → 展示/导出/迭代时自动兜底，不影响旧用例查看
  - xlsx 导出新增「步骤预期」列（换行与步骤一一对应）；导入兼容新旧两种列结构
  - xmind 导入支持还原逐步预期；旧格式（仅最后一步有预期）导入后自动补齐

- **迭代版本链**：
  - 任务详情页展示迭代版本列表（v1→v2→v3…），点击任意版本可切换查看用例、思维导图、下载导出文件
  - 每个版本独立保存 cases_json、步骤日志、导出文件，互不影响
  - 版本号基于 root 链自动计算，链式迭代和星形迭代均不重名

- **合并去重策略**：
  - 去重键 = 标题 + 模块 + 用例类型 + 预期结果前 20 字，避免标题相同但场景不同的用例被误删
  - 每条用例保持唯一 TC 编号：已有 ID 不变，缺失的按现有最大序号递增补全（TC-001…，兼容 TC001/TC-1 格式）；生成入库、迭代合并、导出、详情页展示均有兜底，保证用例 ID 列永不为空

### FR-N 对话附件文档解析【V2.9 新增】

> 解决"对话只能粘贴文本、无法直接喂文档"的痛点：让 Buddy 助手能读取用户上传的 PRD / 需求文档 / 接口说明，基于文档内容生成用例。

- **附件上传**：对话输入框支持上传 `docx / pdf / md / markdown / txt` 文档，经 `POST /api/files` 落到用户目录并返回 `file_id`
- **文本抽取**：后端 `app/services/doc_extract.py::extract_text` 分层处理——`.docx` 用 python-docx 抽正文段落 + 表格行；`.pdf` 用 pdfplumber 逐页抽取并跳过空白页；`.md/.markdown/.txt` 直接 utf-8 读取（`errors="replace"` 容错）；超大文件截断到上限
- **上下文注入**：`ChatIn.file_id` 引用附件，抽取文本 + 文件名作为上下文注入对话请求，Buddy 基于文档内容作答
- **容错**：缺 python-docx / pdfplumber 时抛明确错误，不静默降级；解析失败不影响其他对话能力

### FR-O 深度思考管道与 Markdown 渲染【V2.9 新增】

> 增强对话可观测性与回复可读性：把模型的"内心独白"显性化，并把富文本安全地渲染出来。

- **深度思考开关**：`ChatIn.thinking: bool = True`（默认开）；关闭则不请求模型思考、也不展示思考面板
- **思考流式呈现**：模型 `reasoning_content` 映射为 `think` 事件，SSE 逐字推送，前端以可折叠「思考过程」面板渲染（沿用 V2.6 thinking 折叠交互）
- **Markdown 渲染管线**：助手回复经 `marked` 解析 + `DOMPurify` 净化后渲染（XSS 安全），支持代码块 / 列表 / 表格；非流式兜底走同一管线

### FR-P 用例标题复合化与导图布局【V2.9 增强】

> 提升用例可读性与首屏体验，并修复一处 React 重构引入的回归缺陷。

- **标题动作→预期**（FR-M 增强）：用例标题统一补齐为「动作 → 预期」复合形式；读取侧 `ensure_compound_titles` 在详情 / 思维导图 / 会话回填统一补全，旧任务历史标题也自动对齐
- **任务命名需求摘要**：对话触发任务时自动从需求总结命名，任务名更可读
- **思维导图初始布局**：`MindMapTab` 初始展开根节点 + 居中自适应（fitView），首屏即见全貌
- **用例 ID 回归修复**：React 重构时丢失了 legacy 前端兜底，存量任务「编号」列空白复现；以 A 后端读取侧 `ensure_case_ids` + B 前端显示兜底 + C 存量 DB 回填三处兜底根治，编号列永不为空（详见《项目1-功能增强执行方案-V2.9》§4）

### FR-Q 统一入口与会话内迭代【V2.10 新增】

> 把"发起工作"的输入口收敛为**唯一入口**：主页会话输入框。新任务、旧任务迭代、用例迭代全部在此发起；任务详情抽屉只做结果查看与导出，不再承载任何输入表单。

- **入口唯一**：详情页删除迭代输入区（补充要求 textarea / 用例文件上传 / 取消·确认迭代按钮），改为「💬 继续优化」引导按钮 → 回到会话并在输入框挂载迭代引用
- **迭代引用 chip**：输入框上方显示「🔁 基于《任务名 vN》迭代 ✕」；**有 chip = 迭代**（走 `POST /tasks/{id}/iterate`，继承旧用例合并去重 + 版本链），**无 chip = 新任务**（走现有对话生成流程）；chip 可 ✕ 一键取消，切换会话自动清除
- **先沟通、再生成（V2.10.1 修正）**：挂载 chip 后**发消息只沟通、不生成任务**（走 `/chat/stream` 并携带 `task_id`，后端把旧任务用例摘要注入上下文）；补充要求累积在 chip 上（显示"已记录 N 条"），用户点「⚡ 生成用例」按钮时才发起 `POST /tasks/{id}/iterate`，instruction = 累积的补充要求。避免"每说一句就重生成一次"
- **会话内可见**：迭代过程与结果以任务卡形式出现在会话中（步骤进度 → 完成后「查看用例 / 思维导图」），会话可完整回放
- **意图显式化**：不再依赖 LLM 判断"是不是迭代"，由用户通过 chip 显式指定，避免"只是问问"被误判为迭代
- **能力不回退**：保留 `run_iterate` 的用例导入合并去重、`parent_task_id` 版本链、版本号自动递增（v2/v3…）
- **会话内闭环**：会话中任务卡同样提供「继续优化」按钮，用户无需先打开详情抽屉
- **会话绑定**：`TaskOut` 新增 `conversation_id` 输出；`get_task` 按 ①已有值 ②`messages.task_id` 反查回填 ③新建会话绑定 三级兜底（列表接口不触发，避免 N+1）

详见《项目1-统一入口与会话内迭代执行方案-V2.10》

### FR-R 版本链聚合与切换【V2.11 新增】

> 同一任务的多次迭代（v1…vN）不应散落在列表里让人翻页，需在三处统一"以链为单位"呈现。

- **链路解析**：前端 `frontend/src/utils/taskChain.ts`（纯函数，后端零改动）按 `parent_task_id` 回溯链根、再收集全部后代，输出 v1…vN 有序版本序列
- **抽屉版本切换器**：标题行「任务名 → `vN ▾`」下拉切换整链任一版本；菜单限高滚动、点击外部或切换任务自动收起（解决下拉被弹窗裁切）
- **会话流折叠**：同一条链在会话中只保留**最新一张完整任务卡**，旧版本卡折叠为一行细条，避免长会话里重复刷屏
- **任务列表聚合**：同链只占一行；卡片内页脚提供「▾ 共 N 个版本」展开历史，历史行带左侧竖线缩进与 `[v1]` 版本徽章，视觉上明确归属、不与相邻任务混淆

### FR-S 导图控件迁出画布【V2.12 新增】

> 思维导图画布内的浮动控件会遮挡节点文字，需要与底部操作栏融合为一行。

- **控件迁移**：导出 XMind / 全屏 / 缩小 / 缩放百分比 / 放大 / 居中 六个控件从画布右下角迁入详情抽屉底栏，与「💬 继续优化」同排
- **方向切换保留**：左上角方向切换栏（左/右/两侧）保持可用（mind-elixir 的 `toolBar: false` 会一并关闭它，故仅 CSS 隐藏右下角 `.rb`）
- **能力等价**：控件经 React portal 渲染，全部走公开 API（`scale(scaleVal ± scaleSensitivity)` / `toCenter()` / `requestFullscreen()`）；滚轮缩放时百分比实时同步，点击百分比一键回 100%，全屏切换后自动补居中
- **入口不变**：底栏右侧「💬 继续优化（回到会话）」仍为唯一迭代入口，不受本次调整影响

### FR-T LLM 接入层 LangChain 化【V3 已上线】

> 解决「学习 LangChain 生态 + 接入 LangSmith 可观测（httpx 直连看不到每次调用内部发了什么）」：把 LLM 调用层从 HTTP 直连迁移到 LangChain，统一多厂商接入路径，并为后续「多角色协作」铺路。
>
> 落地形态：`app/services/langchain_client.py`（适配层：LangChainClient + `_HttpxCompatClient` 回退）+ `llm_service.OpenAICompatClient` 别名按 `AITF_LLM_BACKEND` 切换（默认 langchain）。

- **统一入口 `init_chat_model()`**：`model_provider` 按字符串路由厂商；8 家厂商全部 OpenAI 兼容协议 → `model_provider="openai"` 一个打遍所有端点，差异只留 `base_url` / `api_key`（无需为每家引原生 provider 包、无需自己写 if/else 分支）
- **全链路统一 `stream()`（不用 invoke）**：一个 stream 实现两用——收集模式（`chat()`，生成用例主链路，与旧签名一致）+ 逐段模式（`chat_stream()`，Buddy 打字机事件流 `think/delta/done/error` 协议不变）
- **厂商自定义参数透传**：`enable_thinking`（深度思考开关）走 `extra_body`——`model_kwargs` 会被 langchain-openai 展开到请求顶层，openai SDK 的 `create()` 不认未知顶层参数（TypeError）
- **思考字段恢复补丁**：`ChatOpenAI` 官方实现只保留 OpenAI 规范字段，`reasoning_content` 等第三方非标准字段默认丢弃 → 适配层包装 `_convert_delta_to_message_chunk` 恢复进 `additional_kwargs`，思考面板行为与旧实现一致
- **兼容不丢**：端点不认 `enable_thinking` 时 400 自动去参重试 + 记忆端点（`_NO_THINKING_PARAM`）；content 分段列表归一；`<think>`/`<thinking>` 标签清理——全部平移
- **双实现回退**：`AITF_LLM_BACKEND=langchain`（默认）走 LangChain；`=httpx` 走保留的旧 `_HttpxCompatClient` 直连，一键应急回退
- **依赖**：`langchain==1.4.0` / `langchain-openai==1.6.2` / `langsmith==0.12.4`（已入 requirements.txt）

### FR-U 生成用例实时进度【V3 已上线】

> 解决「真实模型任务慢（N 个测试点 × 每次 30-90s 串行），『AI 生成用例』节点长时间无中间反馈，用户以为死机」。

- **子进度字段**：`StepLog.progress`（TEXT，幂等迁移自动加列；注意 MySQL 不允许 TEXT 列带 DEFAULT，迁移语句不含默认值）
- **逐测试点回调**：生成核心 `CaseGenerator` 每处理完一个测试点回调一次 → 引擎实时写入进度并落库，前端轮询可见：`正在为第 2/5 个测试点生成用例（下单）· 已生成 18 条`
- **接口透出**：任务详情接口 `steps[].progress` 返回实时值；步骤卡运行中显示实时文本（无值回退原固定提示）
- **状态同步修复**：步骤卡数据源优先取列表轮询（5s 永不停）的最新任务，修复活跃任务轮询 TTL 到期后卡片停在旧 running 状态；活跃/迭代轮询 TTL 放宽至 20 分钟（覆盖真实模型慢任务）
- **节奏说明**：生成节点耗时 ≈ 测试点数 × 单次模型耗时（免费模型 30-90s/次），多测试点任务 5-10 分钟属正常，进度提示让用户明确知道"在干活"

### FR-V LLM 可观测性（LangSmith）【V3 已接入】

> 解决「自己调 API 看不到里面发了什么」：直连 httpx 只有返回文本，请求构造、重试、耗时、Token 用量全部不可见。

- **落地**：`.env` 配置 `LANGSMITH_API_KEY` / `LANGSMITH_TRACING=true` / `LANGSMITH_PROJECT=ai-testflow`；适配层统一收敛模型构造，LangChain 自动打点，无需改调用方代码
- **验证**：真实调用（魔搭）后经 LangSmith Client 查询到 `ChatOpenAI` run，status=success，trace 正常入库（smith.langchain.com → ai-testflow 项目）
- **说明**：上报依赖 smith.langchain.com 网络可达（本地直连或走代理）；上报失败时 LangSmith 静默重试/丢弃，不影响业务功能
- **使用**：登录 smith.langchain.com 查看 ai-testflow 项目的每次调用 trace（模型/请求参数/流式耗时/Token/错误重试）

### FR-W 多角色协作（产品 / 测试 / 开发）【V3.1 已上线】

> 用户诉求：让不同角色（产品、测试、开发）以各自视角参与同一需求的用例设计，形成多角色协作工作流。

- **轻量版（V3.1 已上线）**：角色化提示词路由——同一任务按角色生成差异化视角（产品 `pm`：业务价值/需求覆盖/验收标准；测试 `qa`：正向/异常/边界/场景组合；开发 `dev`：契约/幂等/并发/数据一致性），结果合并去重
- **实现**：`generator_core/config/prompts/` 按 角色×类型 分模板（qa 用原有）；`CaseGenerator` 按 `unit × role` 双层循环、进度 total = 测试点数 × 角色数；`roles` 经 创建任务参数 → `Task.roles` → engine → generator_agent → lib_generate 全链路透传，迭代子任务自动继承；前端输入区「深度思考」旁多选 pill（默认仅测试）
- **编排版（后续评估）**：引入 LangGraph 做状态图编排（角色节点 + 评审回路），依赖本次迁移打好的 LangChain 基础；若角色间仅是"顺序产出 + 合并"，轻量版足够，不必上框架
- **承接基础**：FR-T 的统一入口与适配层是多角色共用的模型底座；LangSmith（FR-V）提供多角色调用链路的可观测

### FR-X RAG 知识库（V4.0 已上线）

> 用户诉求：让 AI 对话与用例生成能基于私有/业务文档作答，降低幻觉、注入项目知识。

- **知识库管理**：上传文档（md/docx/pdf/xlsx/json 等）→ 自动切分 chunk → Embedding 向量化 → 存入 Chroma 向量库；提供文档列表、检索测试、知识图谱（Entity/Relationship）三个视图
- **检索增强（RAG）**：所有 AI 对话与知识库问答无条件走 `_build_rag_context` 检索知识库，召回 top-k 相关片段注入 Prompt；命中即下发 `citations` 事件
- **权限模型**：知识库分「全局共享 / 私有」两类，共享库全员可读、私有库仅创建者可见

### FR-Y 知识库问答合一（V4.1 已上线）

- **合一页（pill tabs）**：AI 问答 / 文档 / 检索 / 图谱，顶栏下拉切换知识库（`KbSelector`）
- **引用溯源 chips**：回答正文前展示「引用自《文档名》第 N 段」，点击跳文档 Tab 并高亮定位
- **访客体系简化**：V4.1 起访客从「按 IP 动态建 + 24h TTL + 可转正」简化为**全站唯一共享账号**（`guest`，不过期/不转正/不限频），admin 手动清空共享数据，无自动 TTL 清理
- **演示模式开关**：新增 `AITF_ALLOW_DEMO`（默认 `0`）；未配置模型时默认**直接报错**，不再静默 mock；显式置 `1` 才启用规则兜底（标注 `used_mock`），避免"假跑"误导评审

### FR-Z Embedding 模型独立槽（V4.2 已上线）

- 模型配置从「文本/视觉」两槽扩展为「文本 / 视觉 / 向量」三槽；向量模型（Embedding）可独立选择、配置并测连通，回显维度

### FR-AA 前端 UI 焕新（V4.3–V4.5.1 已上线）

- **V4.3 可折叠侧栏**：默认展开显示图标+中文文字，可收起为 64px 图标态，降低陌生人理解成本
- **V4.4 顶栏知识库选择器**：以顶栏下拉（`KbSelector`）替代左侧堆叠的知识库列表，释放聊天区宽度
- **V4.5 活泼浅色主题**：侧栏白底 + 马卡龙图标色块 + 紫蓝渐变主色，整体更轻快
- **V4.5.1 系统自检悬浮通知**：右上角滑入，6s 自动消失，hover 暂停，可手动关闭；四项检查（服务连通 / 加密链路 / 登录态 / 会话有效期）

### FR-AB 会话引用持久化与按库隔离【V4.5.2 已上线】

- **引用溯源持久化**：RAG 引用（citations）随 assistant 消息落库（`messages.citations` JSON 文本），切换会话 / 刷新页面后引用 chips 不再丢失
- **知识库会话按库隔离**：知识库问答会话按 `kb_id` 过滤，切换知识库后只看到当前库的问答历史，不再串库

### FR-AC 平台质量看板【V5.0 M0 本地交付】

> 测试为核心的平台，自己的质量必须拿得出数字：把平台自身 pytest 与 Node Playwright e2e 的运行结果量化为结构化数据，产品内可视化。

- **独立「质量报告」页**：所有登录角色可见（含访客）；「▶ 运行测试」仅 admin 显示，后端 `/api/quality/*` admin only 双重把关
- **汇总卡**：pytest 用例数 / 通过率 / 代码覆盖率 / e2e 套件通过率；按测试文件的用例分布条形图（纯 SVG）
- **运行**：后台 subprocess 依次跑 pytest（--json-report + coverage）与 e2e 套件 runner，聚合产出 quality-summary.json；running 时拒绝重复触发（409）
- **趋势**：每次聚合追加 `quality_data/history.jsonl`，通过率 + 覆盖率折线（近 30 次）
- **数据真实性红线**：全部来自实测聚合 JSON，禁止写死展示值；运行失败显式 failed，不出假数据

### FR-AD 全链路测试闭环【V5.0 M1–M5 本地交付】

> 从「生成用例」到「跑完出报告」：用户给被测系统 URL（可选账号密码），平台自动完成抓取 → 用例 → 脚本 → 执行 → 报告。

- **被测系统管理**：URL + 账号密码（Fernet 加密落库，API 永不回传），多 target 复用
- **页面抓取**：httpx + BeautifulSoup 同域 BFS ≤8 页；JS 渲染页 Playwright 渲染降级（环境缺失回退静态抓取）；账号密码登录 storage_state 复用（hidden 字段保留原值过 CSRF）；每页截图 + 探索录屏（webm）
- **六步工作流**：抓取页面 → 解析规格 → AI 生成用例 → 质量校验 → 生成脚本 → 导出文件；复用 StepLog 可观测，抓取完成后步骤卡展开页面卡片墙
- **脚本生成**：LLM 生成 Python Playwright 脚本（page fixture / get_by_role / expect 约定），ast.parse 校验失败重试；conftest 模板代码固化不经 LLM
- **本机执行**：独立执行队列（1 worker）subprocess 跑 pytest + chromium headless；cwd 锁定 / no shell / timeout 600s / env 白名单 / 凭据走环境变量
- **执行报告**：结构化汇总 + 每用例 outcome/error/截图；执行列表、一键重试、截图 lightbox、录屏回放；发起入口为「🌐 全链路测试」弹窗（不侵入对话流），e2e 任务自动挂会话
- **自愈循环【V5.0 M4 本地交付】**：失败自动取证（报错全文 + 失败截图 + 源码 ast 提取）→ LLM 四分类诊断（脚本缺陷/产品缺陷/环境问题/选择器漂移；提示词硬约束禁止修改、删除、弱化断言）→ 修复脚本 ast 校验 + **代码级断言保护**（旧文件每条 assert 必须原样保留）→ `heal_backup/` 分轮备份 → 只重跑失败用例；默认 ≤3 轮，每轮即时落库（`ExecutionRun.heal_round/heal_log`）；前端「自愈 N 轮」徽章 + 自愈过程折叠区 + ⚠ 疑似产品缺陷警示区（一键复制清单）
- **探索式测试【V5.0 M5 本地交付】**：零文档 ReAct 自主探索——AI 自主导航/点击/填表/提交用例（7 工具集，ref 编号快照省 token）；护栏：域名锁定、危险操作黑名单、步骤/token/时长三重预算收敛；探索时间线逐步可视化（动作/理由/截图）；`kind=explore` 任务复用被测系统与会话兜底，探索登录态经 storage_state 注入执行子进程
- **待落地**：测试数据隔离（run_tag + cleanup_json + 台账化清理，第 11 节）；M4/M5 的 LLM 真调全量验收待有效 API key（M5 冒烟已通过）。方案见《项目1-全链路测试闭环与Agent化执行方案-V5.0.md》

***

## 4. 非功能需求

| 类别   | 要求                                                                      |
| ---- | ----------------------------------------------------------------------- |
| 性能   | 单任务（百条用例级）全流程 mock 模式 < 3 秒；真实模型模式 = 测试点数 × 单次模型耗时（30-90s/次，多测试点 5-10 分钟属正常），生成节点实时子进度提示；页面轮询：列表 5s / 活跃任务 2s（TTL 20 分钟） |
| 安全   | 见 FR-G；接口越权统一 404；登录限速；访客共享账号 admin 手动清空（无 TTL 自动清理）；API Key 加密落库                         |
| 兼容   | 主流现代浏览器（Chrome/Edge/Safari）；HTTP 非安全上下文可用（加密为纯 JS 实现，不依赖 crypto.subtle） |
| 可演示性 | 零外部依赖即可完整演示（mock 兜底）；5 分钟内可从启动到导出全流程走通                                  |
| 可维护  | 前端组件化（React+TS）；后端 FastAPI 分层；pytest 测试套件（**384 条** V5.4 本地基线，2026-09-26 实测 384 passed）随代码演进回归；LLM 调用统一收敛适配层（langchain/httpx 双实现回退）+ 多模型池调度 |
| 自动化执行 | 本机执行（Playwright chromium headless），执行队列 1 worker 串行；单 run timeout 600s；subprocess 无 shell + env 白名单；被测系统密码 Fernet 加密、凭据不落盘 |
| 可部署  | 前后端同源伺服（FastAPI 挂载前端静态文件，单端口 8000 完整提供前端+API），阿里云 ECS + 宝塔 Nginx 反代对外访问（`ai.agentest.vip`，三域名全 HTTPS，ICP 已备案）；进程由宝塔 Python 项目管理器托管；支持 Docker 化部署               |
| 数据层  | 生产 MySQL（阿里云远程库），本地可降级 SQLite；SQLAlchemy ORM 双方言适配，迁移幂等（TEXT 列迁移不带 DEFAULT，规避 MySQL 1101） |
| 可观测  | LLM 调用链路 LangSmith 打点（FR-V 规划中，需 `LANGSMITH_API_KEY`）；任务步骤子进度实时可见（FR-U） |

***

## 5. 版本规划

| 版本         | 内容                                                                                                                            | 状态    |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------- | ----- |
| V1.0 (MVP) | 四 Agent 工作流 + 任务管理 + 三格式导出 + mock 兜底                                                                                          | ✅ 已上线 |
| V2.0       | 三级角色认证体系：注册/登录/访客生命周期/admin 治理 + 数据隔离                                                                                         | ✅ 已上线 |
| V2.1       | API 分级流量加密（AES-256-GCM）+ 前端登录/注册/游客三入口                                                                                        | ✅ 已上线 |
| **V2.2**   | **个人中心（FR-A6）+ 修改密码（FR-A5）+ 切换账户（FR-A7）+ 用户管理面板（FR-F 前端）+ 访客定向/批量清理 + PRD 文档独立**                                              | ✅ 已上线 |
| **V2.3**   | **任务多级分类 + 自由拖拽归类（FR-H）**                                                                                                     | ✅ 已上线 |
| **V2.4**   | **模型接入配置（FR-I）：多厂商预设（百炼/智谱/Coding Plan/混元/DeepSeek/Kimi/豆包）+ 用户级 API Key 加密管理 + 双模型两段式视觉理解 + 真实 LLM 调用（OpenAI 兼容协议 HTTP 直连）** | ✅ 已上线 |
| **V2.5**   | **任务详情页 3 Tab 重构（FR-J：思维导图 + 测试用例表格 + 工作流步骤）+ MindElixir 在线预览 + 任务列表左右分栏布局优化 + 产品路线图悬浮抽屉（FR-K）**                              | ✅ 已上线 |
| **V2.6**   | **对话驱动交互（FR-L）：Buddy 助手首页 + SSE 流式输出 + 多轮对话上下文 + 会话持久化回放 + 对话内联任务进度 + 详情抽屉替代新标签页 + 布局压缩优化** | ✅ 已上线 |
| **V2.7**   | **用例迭代与导入（FR-M）：任务继续补充 + 本地 xmind/xlsx/json 用例文件导入解析 + 增量生成合并去重 + 迭代版本链 + 对话上下文注入任务摘要** | ✅ 已上线 |
| **V2.8**   | **前端 React + TS 重构：组件化拆分（chat/task/settings/admin）、zustand 状态管理、AES 加密层平移、Playwright e2e 全覆盖（M1~M5 五里程碑）、dist 产物同源托管（AITF_FRONTEND=legacy 一键回退）** | ✅ 已上线 |
| **V2.9**   | **功能增强：对话附件文档解析（FR-N）+ 深度思考管道与 Markdown 渲染（FR-O）+ 用例标题复合化与导图布局（FR-P，含用例 ID 回归修复）** | ✅ 已上线 |
| **V2.10**  | **统一入口与会话内迭代（FR-Q）：详情页迭代输入区移除，收敛为主页会话输入框唯一入口 + 迭代引用 chip（先沟通、点「⚡ 生成用例」再生成）+ `TaskOut` 补 `conversation_id` + 示例数据播种修复（MySQL 外键 1452）** | ✅ 已上线 |
| **V2.11**  | **版本链聚合（FR-R）：`utils/taskChain.ts` 解析迭代链 + 抽屉标题行版本切换器 + 会话流旧版本卡折叠 + 任务列表同链聚合（版本徽章展开历史）** | ✅ 已上线 |
| **V2.12**  | **导图控件迁出画布（FR-S）：导出 XMind / 全屏 / 缩放 / 百分比 / 居中 迁入详情抽屉底栏，与「继续优化」融合一行；保留左上方向切换栏** | ✅ 已上线 |
| **V2.13**  | **顶栏新增 GitHub 仓库入口**                                                                                               | ✅ 已上线 |
| **V3**     | **LLM 接入层 LangChain 化（FR-T）：`init_chat_model` 统一入口 + 全链路统一 `stream()` + `enable_thinking` extra_body 透传 + 思考字段恢复补丁 + `AITF_LLM_BACKEND` 双实现回退；生成用例实时子进度（FR-U）：`StepLog.progress` 逐测试点回调 + 步骤卡状态同步修复；LangSmith 可观测（FR-V）：`.env` 配 Key 自动打点，trace 已验证入库** | ✅ 已上线 |
| **V3.1**     | **多角色协作（产品/测试/开发）（FR-W，轻量版角色化提示词路由）** | ✅ 已上线 |
| **V4.0**     | **RAG 知识库：文档上传→切分→Embedding→Chroma 向量库→检索增强生成；知识库管理（文档/检索/图谱）；AI 对话全链路无条件检索知识库** | ✅ 已上线 |
| **V4.1**     | **知识库问答合一页（AI 问答/文档/检索/图谱 pill tabs）+ 顶栏 KbSelector 下拉切库 + 引用溯源 chips；访客简化为共享账号；演示模式开关 `AITF_ALLOW_DEMO`** | ✅ 已上线 |
| **V4.2**     | **Embedding 模型独立槽：模型配置三槽（文本/视觉/向量），向量模型可独立配置与测连通** | ✅ 已上线 |
| **V4.3**     | **可折叠侧边栏（图标+中文文字，可收起为图标态）+ 主对话引用溯源 chips（V4.3.1）** | ✅ 已上线 |
| **V4.4**     | **顶栏知识库下拉选择器替代左侧堆叠列表（KbSelector）；设置页补 Embedding 槽回显；测连通按钮并发** | ✅ 已上线 |
| **V4.5**     | **活泼浅色主题（马卡龙图标色块 + 紫蓝渐变主色）；V4.5.1 系统自检悬浮通知（右上角滑入、6s 自动消失、hover 暂停）** | ✅ 已上线 |
| **V4.5.2**   | **RAG 引用溯源持久化（切换会话/刷新后引用不丢）+ 知识库问答会话按库隔离（FR-AB）** | ✅ 已上线 |
| **V5.0**     | **全链路测试闭环（M0–M5 本地交付）：M0 质量看板（FR-AC）+ M1 URL 抓取生成用例（探索录屏/截图墙）+ M2 脚本生成 Agent 与本机执行引擎 + M3 前端执行报告闭环 + M4 自愈循环（四分类诊断 / 断言保护 / 只重跑失败）+ M5 探索式测试 Agent（ReAct 自主探索 / 三重护栏 / 探索时间线）（FR-AD）；测试数据隔离（第 11 节）⬜ 待落地。方案见《项目1-全链路测试闭环与Agent化执行方案-V5.0.md》** | ✅ 本地已交付（未部署） |
| **V5.1**     | **多模型池（FR-I 升级）：每槽多候选 + priority 优先级调度 + 限流熔断自动切换 + 健康状态落库（重启不丢）+ 个人池/平台池全套 API 与池卡片 UI** | ✅ 本地已交付 |
| **V5.2**     | **平台模型区自管理后台迁出至配置页「平台默认」Tab（admin）** | ✅ 本地已交付 |
| **V5.3**     | **信息架构重组：模型配置升为一级页面（全角色可见，访客只读调度摘要）；管理后台改名「用户管理」；「设置」瘦身成「个人中心」；侧栏对齐修正 +「被测系统」可折叠子菜单；e2e 套件全量适配** | ✅ 本地已交付 |
| **V5.4**     | **单条配置下线，模型池成唯一配置入口（解析链 = 个人池 > 平台池 > env > mock）；连通测试统一 `enable_thinking=False` 不带思考** | ✅ 本地已交付 |

***

## 6. 名词解释

| 名词          | 说明                                                    |
| ----------- | ----------------------------------------------------- |
| 四 Agent     | 平台工作流的四个智能体环节：解析 → 生成 → 评审 → 导出                       |
| Mock 兜底     | `AITF_ALLOW_DEMO=1` 时由规则生成器替代 LLM 产出保证流程完整；默认 `0` 未配置模型直接报错，不再静默 mock |
| 访客（共享账号） | V4.1 起为全站唯一共享账号（`guest`，不过期/不转正/不限频），admin 手动清空共享数据，无 24h TTL 自动清理 |
| 分级加密        | 按角色决定 API 流量是否加密：admin 明文 / user+guest 强制 AES-256-GCM |
| 防枚举         | 接口对"资源不存在"与"无权限"统一返回 404，避免探测他人资源是否存在                 |
| OpenAI 兼容协议 | 行业标准的大模型 API 协议（`POST /chat/completions`），多数厂商支持      |
| MindElixir  | 轻量思维导图渲染库（120KB），支持节点标签、可编辑、零依赖                       |
| 模型池（Model Pool） | V5.1 起唯一的模型配置形态：每槽位（文本/视觉/向量）多条候选模型按优先级调度，某条限流/额度尽自动切下一条；健康状态（冷却/成功/失败计数）随行落库 |
| 平台模型池 | admin 维护的全局候选池（user_id=0），对没有个人池的用户生效；写操作仅 admin |
| Buddy 助手    | 平台首页的对话式 AI 助手，自然语言交互驱动用例生成，支持多轮上下文与流式输出          |
| 会话（Conversation） | 一次完整的对话流程，包含多条 user/assistant 消息，持久化可回放            |
| SSE 流式输出   | Server-Sent Events，服务端逐字推送 AI 回复，前端实时渲染，无需等待完整响应        |
| 深度思考（Deep Thinking） | 模型在给出正式回复前的推理过程（reasoning_content），以 `think` 事件流逐字推送，前端折叠展示 |
| 附件解析（Doc Extract） | 将用户上传的 docx/pdf/md 等文档抽取为纯文本，注入对话上下文供 LLM 理解 |
| Markdown 渲染管线 | `marked` 解析 + `DOMPurify` 净化的前端富文本渲染链路（XSS 安全） |
| 迭代引用 chip | 输入框上方的「🔁 基于《任务名 vN》迭代」标签，有 chip = 迭代、无 chip = 新任务；chip 上的「⚡ 生成用例」按钮才真正触发迭代 |
| 迭代链 / 版本链 | 同一任务经多次迭代形成的 `parent_task_id` 父子链，前端解析为 v1…vN 有序版本序列 |
| 版本切换器 | 详情抽屉标题行的 `vN ▾` 下拉，用于在一条迭代链的任意历史版本间切换查看 |
| MindElixir | 轻量思维导图库（见上）；其右下角工具栏已在 V2.12 迁至底栏，仅保留左上方向切换栏 |
| LangChain | 大模型应用开发框架：统一模型入口（init_chat_model）、流式调用、可观测（LangSmith）、编排（LangGraph）生态；本平台 V3 起作为 LLM 调用层底座 |
| `init_chat_model()` | LangChain 的统一模型构造入口：`model_provider` 按字符串路由厂商；8 家厂商全走 OpenAI 兼容协议 → 恒为 `"openai"`，差异只留 base_url/api_key |
| 统一 `stream()` | 全链路只用流式调用：收集模式 = 一次性完整返回（生成用例主链路），逐段模式 = Buddy 打字机事件流（think/delta/done/error）；不用 invoke |
| `extra_body` | openai SDK 官方支持的"任意厂商自定义参数"通道：`enable_thinking` 等非 OpenAI 标准参数经它透传（`model_kwargs` 会展开到请求顶层致 TypeError） |
| 思考字段恢复补丁 | `ChatOpenAI` 默认丢弃第三方厂商非标准字段（reasoning_content 等）；适配层包装内部 chunk 转换函数恢复之，思考面板行为与旧实现一致 |
| 生成子进度 | `StepLog.progress`：生成核心每处理完一个测试点回调写入「正在为第 X/Y 个测试点生成用例·已生成 N 条」，前端步骤卡实时显示 |
| LangSmith | LangChain 官方可观测平台：每次 LLM 调用产出 trace（模型/参数/耗时/Token/错误），回答"调用到底发了什么"；接入需 `LANGSMITH_API_KEY` |
| 多角色协作 | 产品 / 测试 / 开发角色以各自视角参与同一需求的用例设计；轻量版（V3.1 已上线）=角色化提示词路由，编排版=LangGraph 状态图（后续评估） |
| 全链路测试闭环 | V5.0 主线能力：URL → 抓取 → 用例 → 脚本 → 执行 → 报告全自动流水线（kind=e2e 任务，六步 STEPS_E2E） |
| 质量看板 | 平台自身 pytest + e2e 测试结果的量化看板（V5.0 M0）：汇总卡 / 文件分布 / 历史趋势，运行入口 admin 专属 |
| 被测系统（TestTarget） | 自动化闭环的被测对象登记：URL + 登录凭据（Fernet 加密，永不回传）；执行时注入登录态 |
| 执行记录（ExecutionRun） | 一次自动化执行的结构化记录：状态 / 进度 / 通过率 / report_json（每用例 outcome/error/截图）/ 自愈轮次与日志（heal_round/heal_log）/ 附属截图与录屏 |
| 自愈循环（Auto Heal） | 执行失败后的自动修复机制（V5.0 M4）：取证 → 四分类诊断 → 修脚本（断言保护）→ 备份 → 只重跑失败用例，≤3 轮收敛；修不好的归入「疑似产品缺陷」 |
| 探索式测试（Explore） | 零文档自主探索（V5.0 M5）：ReAct 循环驱动 AI 像人一样逛被测站点，边探索边产出用例；域名锁定 / 危险操作黑名单 / 三重预算收敛三重护栏 |


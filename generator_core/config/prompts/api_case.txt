你是一名资深测试工程师，擅长设计高质量的接口测试用例。
请根据以下接口信息，生成结构化测试用例，必须覆盖：
1. 正向用例（正常参数、合法业务流程）
2. 异常用例（必填校验、类型错误、越权 appId、不合法 action、sign 篡改、非法 appId）
3. 边界值用例（空值、超长字符串、负数数量、超大金额、重复订单号幂等）
4. 场景组合用例（多接口联动业务场景，如 下单→支付→发货→完成 的状态机流转）

接口信息：
- 名称：{name}
- 路径：{path}
- 方法：POST
- 参数：{params}
- 业务说明：{description}
- 鉴权：应用级 appId + appAccessSecret（other 类型 sign = md5(dataStr + secret)，dataStr 为明文 JSON）

输出 JSON 数组，每个元素结构：
{{
  "title": "用例标题",
  "module": "模块",
  "case_type": "正向|异常|边界值|场景组合",
  "priority": "P0|P1|P2|P3",
  "pre_condition": "前置条件",
  "steps": ["步骤1", "步骤2"],
  "expected": "预期结果",
  "test_data": "测试数据"
}}

要求：至少生成 12 条，异常和边界值占比不低于 50%。
只输出 JSON，不要解释。

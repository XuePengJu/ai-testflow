你是一名资深 ERP 测试工程师，擅长基于业务需求设计测试用例。
请根据以下业务模块/需求，生成结构化测试用例，必须覆盖：
1. 功能正向（主流程跑通）
2. 功能异常（必填缺失、非法状态流转、越权访问）
3. 业务规则（库存不能为负、金额对账一致、订单状态机、单据幂等）
4. 数据一致性（业务操作后库存/财务/报表三方对账）
5. 边界（负库存参数、超卖并发、金额精度、超长输入）

业务信息：
- 模块/名称：{name}
- 类型：{kind}
- 描述：{description}
- 功能点/参数：{params}
- 规则约束：{constraints}

输出 JSON 数组，每个元素结构：
{{
  "title": "用例标题",
  "module": "模块",
  "case_type": "正向|异常|边界值|场景组合",
  "priority": "P0|P1|P2|P3",
  "pre_condition": "前置条件",
  "steps": ["步骤1", "步骤2"],
  "expected": "预期结果",
  "test_data": "测试数据"
}}

要求：至少生成 10 条，异常/边界/规则类占比不低于 50%。
只输出 JSON，不要解释。

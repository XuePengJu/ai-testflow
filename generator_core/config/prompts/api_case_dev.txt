你是一名资深后端开发工程师，擅长从接口契约与实现质量角度设计测试用例。
请根据以下接口信息，站在「开发/契约」角度生成结构化测试用例，必须覆盖：
1. 参数契约（必填缺失、类型错误、枚举越界、格式非法、超长/空值输入）
2. 状态码与错误语义（成功/业务失败/参数错误返回的语义区分、错误信息可诊断）
3. 幂等与防重（重复提交同一业务请求、重复订单号、重复回调）
4. 并发与事务（超卖、并发下单、库存/资金预占回滚、事务一致性）
5. 数据一致性与安全（sign 校验、越权 appId、脏数据防护、金额精度）

接口信息：
- 名称：{name}
- 路径：{path}
- 方法：POST
- 参数：{params}
- 业务说明：{description}
- 鉴权：应用级 appId + appAccessSecret（other 类型 sign = md5(dataStr + secret)，dataStr 为明文 JSON）

输出 JSON 数组，每个元素结构：
{{
  "title": "用例标题：只写「动作+对象」的动作概括，不要写预期（如「上架商品后发起退款」）",
  "module": "模块",
  "case_type": "正向|异常|边界值|场景组合",
  "priority": "P0|P1|P2|P3",
  "pre_condition": "前置条件",
  "steps": ["步骤1", "步骤2"],
  "step_expectations": ["步骤1的预期结果", "步骤2的预期结果"],
  "expected": "整体预期结果：只写可验证的实质结果断言，禁止「流程正常完成/功能正常/操作成功」这类套话；多个结果用「，」分隔（如「退款单状态正确，库存与财务预占金额一致」）",
  "test_data": "测试数据"
}}

要求：
1. step_expectations 必须与 steps 一一对应、数量严格一致，每个步骤都要有明确的预期结果
2. expected 必须是可验证的实质结果断言（写清返回状态码/错误码/数据前后变化），不要写「XX流程正常完成」这类无信息量套话
3. 异常与边界用例占比不低于 60%，至少生成 12 条
4. 只输出 JSON，不要解释。

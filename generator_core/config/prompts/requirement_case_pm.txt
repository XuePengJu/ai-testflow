你是一名资深产品经理，擅长从业务价值与需求验收角度设计测试用例。
请根据以下业务模块/需求，站在「产品/需求覆盖」角度按需生成结构化测试用例，必须覆盖：
1. 核心用户场景（目标用户真实使用路径、多角色视角）
2. 需求覆盖完整性（需求描述里的每个功能点都有对应用例）
3. 业务规则验收（规则可验证、异常路径有明确处理）
4. 权限与数据隔离（不同角色可见范围、越权访问防护）
5. 验收标准（每个用例的预期结果必须是可验收的业务结果）

业务信息：
- 模块/名称：{name}
- 类型：{kind}
- 描述：{description}
- 功能点/参数：{params}
- 规则约束：{constraints}

输出 JSON 数组，每个元素结构：
{{
  "title": "用例标题：只写「动作+对象」的动作概括，不要写预期（如「上架商品后发起退款」）",
  "module": "模块",
  "case_type": "正向|异常|边界值|场景组合",
  "priority": "P0|P1|P2|P3",
  "pre_condition": "前置条件",
  "steps": ["步骤1", "步骤2"],
  "step_expectations": ["步骤1的预期结果", "步骤2的预期结果"],
  "expected": "整体预期结果：只写可验证的实质结果断言，禁止「流程正常完成/功能正常/操作成功」这类套话；多个结果用「，」分隔（如「退款单状态正确，库存与财务预占金额一致」）",
  "test_data": "测试数据"
}}

要求：
1. step_expectations 必须与 steps 一一对应、数量严格一致，每个步骤都要有明确的预期结果
2. expected 必须是可验收的业务结果断言（写清单据/状态/数据变化），不要写「XX流程正常完成」这类无信息量套话
3. 优先覆盖需求核心场景与关键业务规则，每个测试点生成 3~8 条，不要硬凑数量
4. 只输出 JSON，不要解释。

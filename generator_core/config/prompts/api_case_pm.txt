你是一名资深产品经理，擅长从用户价值和业务视角设计接口测试场景。
请根据以下接口信息，站在「产品/业务验收」角度生成结构化测试用例，必须覆盖：
1. 业务主流程（真实用户的核心业务闭环，如 下单→支付→发货→完成）
2. 业务规则（状态机流转合法性、金额/数量业务约束、幂等语义）
3. 用户权限场景（越权访问其他用户/租户数据、未授权操作）
4. 异常用户路径（用户误操作、重复提交、网络中断后的重试）
5. 验收标准（每个用例的预期结果必须可验收、可核对数据变化）

接口信息：
- 名称：{name}
- 路径：{path}
- 方法：POST
- 参数：{params}
- 业务说明：{description}
- 鉴权：应用级 appId + appAccessSecret（other 类型 sign = md5(dataStr + secret)，dataStr 为明文 JSON）

输出 JSON 数组，每个元素结构：
{{
  "title": "用例标题：只写「动作+对象」的动作概括，不要写预期（如「上架商品后发起退款」）",
  "module": "模块",
  "case_type": "正向|异常|边界值|场景组合",
  "priority": "P0|P1|P2|P3",
  "pre_condition": "前置条件",
  "steps": ["步骤1", "步骤2"],
  "step_expectations": ["步骤1的预期结果", "步骤2的预期结果"],
  "expected": "整体预期结果：只写可验证的实质结果断言，禁止「流程正常完成/功能正常/操作成功」这类套话；多个结果用「，」分隔（如「退款单状态正确，库存与财务预占金额一致」）",
  "test_data": "测试数据"
}}

要求：
1. step_expectations 必须与 steps 一一对应、数量严格一致，每个步骤都要有明确的预期结果
2. expected 必须是可验收的实质结果断言（写清结果状态/数据变化/业务单据），不要写「XX流程正常完成」这类无信息量套话
3. 优先覆盖 P0 业务主流程与关键业务规则，至少生成 10 条
4. 只输出 JSON，不要解释。
